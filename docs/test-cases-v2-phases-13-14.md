# Binny Inventory Management System — Test Cases v2 (Phases 13–14)

**Project:** Binny Footwear — Mahavir Polymers Inventory Management System  
**Built by:** Basiq360  
**Date:** 2026-04-16  
**Backend API Base:** `/api/v1`  
**Frontend URL:** `http://localhost:3000`  
**Roles:** Admin, Supervisor, Warehouse Operator, Dispatch Operator  
**Child Box Statuses:** `FREE` | `PACKED` | `DISPATCHED`  
**Master Carton Statuses:** `CREATED` | `ACTIVE` | `CLOSED` | `DISPATCHED`

---

## Table of Contents

- [Phase 13: Negative Tests, Edge Cases & Boundary Values](#phase-13-negative-tests-edge-cases--boundary-values)
  - [Section 52: Input Validation Edge Cases](#section-52-input-validation-edge-cases)
  - [Section 53: State Machine Edge Cases](#section-53-state-machine-edge-cases)
  - [Section 54: Error Handling & Recovery](#section-54-error-handling--recovery)
  - [Section 55: Pagination & Search](#section-55-pagination--search)
- [Phase 14: Full Lifecycle End-to-End Tests](#phase-14-full-lifecycle-end-to-end-tests)
  - [Section 56: Complete Lifecycle — Multi-Role E2E](#section-56-complete-lifecycle--multi-role-e2e)
  - [Section 57: Regression Tests](#section-57-regression-tests)
- [Execution Phase Summary](#execution-phase-summary)

---

## Phase 13: Negative Tests, Edge Cases & Boundary Values

### Section 52: Input Validation Edge Cases

#### 52.1 — String Boundary Tests

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-EDGE-001 | Admin | Create product with article_code exactly 20 chars — boundary valid | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with header `Authorization: Bearer <admin_token>` and body `{"article_name":"BoundaryTest","article_code":"ABCDEFGHIJ1234567890","colour":"Black","size":"8","mrp":299,"category":"Gents","section_id":"<valid_section_id>"}` (article_code is exactly 20 characters) 3. Check response status and body | HTTP 201; product is created successfully; `article_code` in response equals `"ABCDEFGHIJ1234567890"` (20 chars); auto-generated SKU is present in response | API |
| TC-EDGE-002 | Admin | Create product with article_code 21 chars — exceeds limit | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with header `Authorization: Bearer <admin_token>` and body `{"article_name":"BoundaryTest","article_code":"ABCDEFGHIJ12345678901","colour":"Black","size":"8","mrp":299,"category":"Gents","section_id":"<valid_section_id>"}` (article_code is exactly 21 characters) 3. Check response status and body | HTTP 400; response body contains a validation error referencing `article_code` field with message such as `"must be at most 20 characters"`; no product is created in DB | API |
| TC-EDGE-003 | Admin | Create product with article_name of 1 char — minimum valid | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with body `{"article_name":"A","article_code":"MIN001","colour":"Red","size":"6","mrp":199,"category":"Ladies","section_id":"<valid_section_id>"}` 3. Check response status and body | HTTP 201; product is created successfully; `article_name` in response equals `"A"`; product is retrievable via GET | API |
| TC-EDGE-004 | Admin | Create product with empty article_name — required field missing | P0 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with body `{"article_name":"","article_code":"ART999","colour":"Blue","size":"7","mrp":249,"category":"Gents","section_id":"<valid_section_id>"}` 3. Check response status and body | HTTP 400; response body contains validation error referencing `article_name` with message such as `"article_name is required"` or `"must not be empty"`; no product created | API |
| TC-EDGE-005 | Admin | Create customer with firm_name of 255 chars — max boundary | P2 | 1. Login as Admin, obtain token 2. Construct a string of exactly 255 characters: `"A"` repeated 255 times 3. POST `/api/v1/customers` with body `{"firm_name":"<255-char-string>","address":"123 Main St","mobile":"9876543210","customer_type":"Primary Dealer"}` 4. Check response status | HTTP 201 if 255 chars is within the DB column limit; OR HTTP 400 if the column max is lower than 255. Either way: no 500 server error; a clear validation message if rejected | API |
| TC-EDGE-006 | Admin | Create customer with GSTIN exactly 15 chars — valid format | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/customers` with body `{"firm_name":"ValidGSTIN Co","address":"Delhi","mobile":"9876543210","gstin":"22AAAAA0000A1Z5","customer_type":"Primary Dealer"}` (GSTIN is exactly 15 chars and matches standard Indian GSTIN format: 2-digit state code + 10-char PAN + 1 entity number + Z + check digit) 3. Check response | HTTP 201; customer created with `gstin` stored as `"22AAAAA0000A1Z5"`; response contains valid customer `id` | API |
| TC-EDGE-007 | Admin | Create customer with GSTIN 14 chars — too short | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/customers` with body `{"firm_name":"ShortGSTIN Co","address":"Mumbai","mobile":"9876543210","gstin":"22AAAAA0000A1Z","customer_type":"Primary Dealer"}` (GSTIN is only 14 characters) 3. Check response status and body | HTTP 400; response body contains validation error referencing `gstin` field, such as `"GSTIN must be exactly 15 characters"` or `"Invalid GSTIN format"`; no customer created | API |
| TC-EDGE-008 | Admin | Create customer with mobile exactly 10 digits — valid | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/customers` with body `{"firm_name":"Mobile Test Co","address":"Pune","mobile":"9999999999","customer_type":"Primary Dealer"}` (mobile is exactly 10 digits) 3. Check response status | HTTP 201; customer created successfully; `mobile` stored as `"9999999999"`; no validation error | API |

#### 52.2 — Numeric Boundary Tests

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-EDGE-009 | Admin | Create product with MRP = 0 — zero value boundary | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with body `{"article_name":"ZeroMRP","article_code":"ZMR001","colour":"White","size":"8","mrp":0,"category":"Gents","section_id":"<valid_section_id>"}` 3. Check response status and body | HTTP 400 if zero MRP is rejected by business rule (footwear must have a price > 0); OR HTTP 201 if zero is technically allowed. Either case: no 500 error; consistent behavior; if 400, response contains message such as `"MRP must be greater than 0"` | API |
| TC-EDGE-010 | Admin | Create product with MRP = 0.01 — minimum positive value | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with body `{"article_name":"MinMRP","article_code":"MMR001","colour":"Black","size":"6","mrp":0.01,"category":"Gents","section_id":"<valid_section_id>"}` 3. Check response status and body | HTTP 201; product created successfully; `mrp` stored as `0.01`; product retrievable via GET with correct MRP value | API |
| TC-EDGE-011 | Admin | Create product with MRP = 999999.99 — large decimal value | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with body `{"article_name":"MaxMRP","article_code":"MXR001","colour":"Gold","size":"9","mrp":999999.99,"category":"Gents","section_id":"<valid_section_id>"}` 3. Check response status and verify stored value | HTTP 201; product created; `mrp` stored as `999999.99` without rounding or truncation; GET `/api/v1/products/<id>` returns `mrp: 999999.99` | API |
| TC-EDGE-012 | Warehouse Operator | Bulk create child boxes with count = 1 — minimum boundary | P1 | 1. Login as Warehouse Operator, obtain token 2. POST `/api/v1/child-boxes/bulk` with body `{"product_id":"<valid_product_id>","count":1}` 3. Check response status and returned array | HTTP 201; response contains array with exactly 1 child box object; child box has status `FREE`; barcode starts with `"BINNY-CB-"` | API |
| TC-EDGE-013 | Warehouse Operator | Bulk create child boxes with count = 500 — maximum boundary | P1 | 1. Login as Warehouse Operator, obtain token 2. POST `/api/v1/child-boxes/bulk` with body `{"product_id":"<valid_product_id>","count":500}` 3. Check response status, array length, and uniqueness of barcodes | HTTP 201; response contains array with exactly 500 child box objects; all 500 have unique barcodes; all have status `FREE`; no duplicates in DB | API |
| TC-EDGE-014 | Warehouse Operator | Bulk create child boxes with count = 501 — exceeds maximum | P1 | 1. Login as Warehouse Operator, obtain token 2. POST `/api/v1/child-boxes/bulk` with body `{"product_id":"<valid_product_id>","count":501}` 3. Check response status and body | HTTP 400; response body contains validation error such as `"count must not exceed 500"`; no child boxes created; DB count for product remains unchanged | API |

#### 52.3 — Special Characters & Injection Tests

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-EDGE-015 | Admin | Create product with article_name containing Unicode/accented chars | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with body `{"article_name":"Étude Señor Über","article_code":"UNI001","colour":"Beige","size":"7","mrp":349,"category":"Gents","section_id":"<valid_section_id>"}` 3. Check response and then GET the product | HTTP 201; product created; GET `/api/v1/products/<id>` returns `article_name` as `"Étude Señor Über"` with no character corruption (no mojibake); DB and API handle UTF-8 correctly | API |
| TC-EDGE-016 | Admin | Create product with article_name containing HTML script tag — no XSS | P0 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with body `{"article_name":"<script>alert(1)</script>","article_code":"XSS001","colour":"Red","size":"8","mrp":199,"category":"Gents","section_id":"<valid_section_id>"}` 3. Check response 4. If stored, navigate to Product list page in browser | HTTP 201 (the string is stored as-is in DB); GET response returns the raw string without server-side modification; when rendered in browser, the text appears as literal text `"<script>alert(1)</script>"` — no JavaScript executes, no alert dialog appears; React's JSX escapes the value | API + E2E |
| TC-EDGE-017 | Any | Login with SQL injection in email field | P0 | 1. POST `/api/v1/auth/login` with body `{"email":"' OR 1=1 --","password":"anything"}` 2. Check response status 3. Check that server returns a structured error and does not crash | HTTP 400 or 401; response is valid JSON with an error message; no 500 Internal Server Error; no DB data is leaked in the response body; the server continues to function normally for subsequent requests | API |
| TC-EDGE-018 | Admin | Search products with `%` SQL wildcard character | P1 | 1. Login as Admin, obtain token 2. GET `/api/v1/products?search=%` with `Authorization: Bearer <admin_token>` 3. Check response status and body | HTTP 200; response is a valid paginated list (possibly empty, possibly matching all); no 500 error; server treats `%` as a literal search string (properly parameterized query); no SQL injection occurs | API |
| TC-EDGE-019 | Admin | Create customer with firm_name containing apostrophe and ampersand | P1 | 1. Login as Admin, obtain token 2. POST `/api/v1/customers` with body `{"firm_name":"O'Brien & Sons Pvt. Ltd.","address":"123 Main St, Delhi","mobile":"9876543210","customer_type":"Primary Dealer"}` 3. Check response and then GET the customer | HTTP 201; customer created with `firm_name` stored as `"O'Brien & Sons Pvt. Ltd."` exactly; GET returns the same value without corruption (apostrophe not escaped or mangled); no SQL error triggered | API |
| TC-EDGE-020 | Any | Trace barcode with URL-encoded special characters | P1 | 1. Login with any role, obtain token 2. GET `/api/v1/trace/BINNY-CB-%00%0A%0D` (URL-encoded null byte and CRLF in barcode path) with `Authorization: Bearer <token>` 3. Check response status | HTTP 400 or 404; server handles the malformed barcode gracefully; no 500 error; no server crash; response is valid JSON with an error message | API |
| TC-EDGE-021 | Admin | POST body with extra unknown fields — fields ignored | P2 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with body `{"article_name":"ExtraField","article_code":"EXT001","colour":"Black","size":"8","mrp":299,"category":"Gents","section_id":"<valid_section_id>","unknownField":"hacker","__proto__":"polluted","admin":true}` 3. Check response status and body | HTTP 201; product is created normally using only the known fields; extra fields (`unknownField`, `__proto__`, `admin`) are silently ignored and NOT stored or reflected in DB; response body does not contain the extra fields; no prototype pollution | API |
| TC-EDGE-022 | Admin | POST body with null values for required fields | P0 | 1. Login as Admin, obtain token 2. POST `/api/v1/products` with body `{"article_name":null,"article_code":null,"colour":null,"size":null,"mrp":null,"category":null,"section_id":null}` 3. Check response status and body | HTTP 400; response body contains validation errors for each null required field; multiple field errors may be returned; no product created; no 500 server error | API |

---

### Section 53: State Machine Edge Cases

#### 53.1 — Child Box State Transitions

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-STATE-001 | Warehouse Operator | FREE child box packed into carton — status becomes PACKED | P0 | 1. Login as Warehouse Operator, obtain token 2. Create a child box via POST `/api/v1/child-boxes/bulk` with `count:1`; note the returned `barcode` (status=FREE) 3. Create a master carton via POST `/api/v1/master-cartons` to get a `carton_id` 4. POST `/api/v1/master-cartons/pack` with body `{"master_carton_id":"<carton_id>","child_box_barcodes":["<barcode>"]}` 5. GET `/api/v1/child-boxes/<child_box_id>` and inspect `status` | HTTP 200 on pack; subsequent GET of the child box returns `status: "PACKED"`; child box `master_carton_id` matches the carton; master carton `status` is now `ACTIVE`; `child_count` is 1 | API |
| TC-STATE-002 | Warehouse Operator | PACKED child box cannot be packed into another carton | P0 | 1. Login as Warehouse Operator, obtain token 2. Create child box (status=FREE) and pack it into Carton A (child box now PACKED) 3. Create a second master Carton B 4. POST `/api/v1/master-cartons/pack` with body `{"master_carton_id":"<carton_B_id>","child_box_barcodes":["<same_barcode>"]}` 5. Check response status | HTTP 400; response body contains error message such as `"Child box is already packed"` or `"Child box status is not FREE"`; child box remains in Carton A (unchanged); Carton B `child_count` remains 0 | API |
| TC-STATE-003 | Warehouse Operator | PACKED child box unpacked — status returns to FREE | P0 | 1. Login as Warehouse Operator, obtain token 2. Create child box and pack it into a carton (status=PACKED) 3. POST `/api/v1/master-cartons/unpack-box` with body `{"child_box_barcode":"<barcode>"}` OR POST `/api/v1/child-boxes/<id>/unpack` depending on API design 4. GET child box to check status | HTTP 200 on unpack; child box `status` is now `FREE`; child box `master_carton_id` is null; master carton `child_count` decremented by 1; if carton had only 1 box, carton status may revert to `CREATED` | API |
| TC-STATE-004 | Any | DISPATCHED child box cannot be unpacked | P0 | 1. Login as Admin, obtain token 2. Set up a dispatched child box: create product → create child box → pack into carton → close carton → create dispatch with carton → verify child box status=DISPATCHED 3. Attempt POST `/api/v1/master-cartons/unpack-box` or equivalent unpack endpoint with the dispatched child box barcode 4. Check response | HTTP 400; response body contains error message such as `"Cannot unpack a dispatched child box"` or `"Child box is already dispatched"`; child box remains DISPATCHED; dispatch record unchanged | API |
| TC-STATE-005 | Any | DISPATCHED child box cannot be repacked | P0 | 1. Set up a dispatched child box (same setup as TC-STATE-004) 2. Create a new master carton 3. POST `/api/v1/master-cartons/pack` with body `{"master_carton_id":"<new_carton_id>","child_box_barcodes":["<dispatched_barcode>"]}` 4. Check response | HTTP 400; response body contains error such as `"Child box has already been dispatched"` or `"Cannot pack a DISPATCHED child box"`; no packing occurs; new carton `child_count` remains 0 | API |
| TC-STATE-006 | Warehouse Operator | FREE child box cannot be unpacked — not in any carton | P1 | 1. Login as Warehouse Operator, obtain token 2. Create a new child box (status=FREE, not packed into any carton) 3. POST the unpack endpoint with body referencing the FREE child box barcode 4. Check response | HTTP 400; response body contains error such as `"Child box is not packed in any carton"` or `"Cannot unpack a FREE child box"`; no state change occurs | API |
| TC-STATE-007 | Warehouse Operator | FREE child box cannot be dispatched directly | P0 | 1. Login as Warehouse Operator (or Admin), obtain token 2. Create a child box (status=FREE) 3. Attempt to create a dispatch that references the FREE child box barcode directly (not via a master carton) 4. Check response | HTTP 400; response body contains error such as `"Child box must be packed in a carton before dispatch"` or `"Invalid child box state for dispatch"`; no dispatch record created | API |
| TC-STATE-008 | Dispatch Operator | Dispatch carton → all PACKED child boxes become DISPATCHED | P0 | 1. Create product, 3 child boxes, pack all 3 into a master carton (status=ACTIVE) 2. Supervisor closes the carton (status=CLOSED) 3. Login as Dispatch Operator, obtain token 4. POST `/api/v1/dispatch` with body `{"master_carton_ids":["<carton_id>"],"customer_id":"<customer_id>"}` 5. GET each of the 3 child boxes and check their `status` | HTTP 201 on dispatch creation; GET on all 3 child boxes returns `status: "DISPATCHED"`; master carton `status` is `DISPATCHED`; dispatch record contains correct `carton_id`, `customer_id`, `dispatch_date`, and `total_pairs` | API |

#### 53.2 — Master Carton State Transitions

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-STATE-009 | Warehouse Operator | CREATED carton + pack first box → status becomes ACTIVE | P0 | 1. Login as Warehouse Operator, obtain token 2. POST `/api/v1/master-cartons` to create a new carton — note returned `id` and verify `status: "CREATED"` 3. Create a child box (status=FREE) 4. POST `/api/v1/master-cartons/pack` with body `{"master_carton_id":"<carton_id>","child_box_barcodes":["<barcode>"]}` 5. GET `/api/v1/master-cartons/<carton_id>` | HTTP 200; GET response shows carton `status` is now `"ACTIVE"`; `child_count: 1`; child box is listed in carton children | API |
| TC-STATE-010 | Supervisor | ACTIVE carton → close → status becomes CLOSED | P0 | 1. Set up an ACTIVE master carton (has at least 1 packed child box) 2. Login as Supervisor, obtain token 3. POST `/api/v1/master-cartons/<carton_id>/close` with `Authorization: Bearer <supervisor_token>` 4. GET `/api/v1/master-cartons/<carton_id>` | HTTP 200 on close; GET response shows carton `status: "CLOSED"`; `child_count` unchanged; child boxes still PACKED; carton `closed_at` timestamp is set | API |
| TC-STATE-011 | Dispatch Operator | CLOSED carton → dispatch → status becomes DISPATCHED | P0 | 1. Set up a CLOSED master carton with packed child boxes 2. Login as Dispatch Operator, obtain token 3. Create a valid customer in DB 4. POST `/api/v1/dispatch` with body `{"master_carton_ids":["<closed_carton_id>"],"customer_id":"<customer_id>"}` 5. GET `/api/v1/master-cartons/<carton_id>` | HTTP 201 on dispatch; GET shows carton `status: "DISPATCHED"`; dispatch record created with `dispatched_at` timestamp; all child boxes in carton are now DISPATCHED | API |
| TC-STATE-012 | Dispatch Operator | ACTIVE carton can also be dispatched directly | P1 | 1. Set up an ACTIVE master carton (not closed) with packed child boxes 2. Login as Dispatch Operator, obtain token 3. POST `/api/v1/dispatch` with body `{"master_carton_ids":["<active_carton_id>"],"customer_id":"<customer_id>"}` 4. GET the carton and child boxes | HTTP 201; dispatch succeeds for ACTIVE carton (system auto-closes or allows direct dispatch); carton `status` becomes `DISPATCHED`; all child boxes become DISPATCHED; dispatch record is created | API |
| TC-STATE-013 | Supervisor | DISPATCHED carton cannot be closed | P0 | 1. Set up a DISPATCHED master carton 2. Login as Supervisor, obtain token 3. POST `/api/v1/master-cartons/<dispatched_carton_id>/close` with `Authorization: Bearer <supervisor_token>` 4. Check response | HTTP 400; response body contains error such as `"Cannot close a dispatched carton"` or `"Carton is already dispatched"`; carton status remains DISPATCHED | API |
| TC-STATE-014 | Warehouse Operator | DISPATCHED carton cannot receive more child boxes | P0 | 1. Set up a DISPATCHED master carton 2. Login as Warehouse Operator, obtain token 3. Create a new FREE child box 4. POST `/api/v1/master-cartons/pack` with body `{"master_carton_id":"<dispatched_carton_id>","child_box_barcodes":["<new_barcode>"]}` 5. Check response | HTTP 400; response body contains error such as `"Cannot add boxes to a dispatched carton"`; new child box remains FREE; dispatched carton unchanged | API |
| TC-STATE-015 | Warehouse Operator | DISPATCHED carton boxes cannot be individually unpacked | P0 | 1. Set up a DISPATCHED master carton containing child boxes 2. Login as Warehouse Operator, obtain token 3. Attempt the unpack-box endpoint for one of the dispatched child boxes in the carton 4. Check response | HTTP 400; response body contains error about the dispatched state; child box remains DISPATCHED; carton remains DISPATCHED | API |
| TC-STATE-016 | Warehouse Operator | DISPATCHED carton cannot be full-unpacked | P0 | 1. Set up a DISPATCHED master carton 2. Login as Warehouse Operator, obtain token 3. POST `/api/v1/master-cartons/<dispatched_carton_id>/full-unpack` with `Authorization: Bearer <warehouse_token>` 4. Check response | HTTP 400; response body contains error such as `"Cannot unpack a dispatched carton"`; carton and all child boxes remain DISPATCHED | API |
| TC-STATE-017 | Warehouse Operator | CLOSED carton cannot receive more child boxes | P0 | 1. Set up a CLOSED master carton 2. Login as Warehouse Operator, obtain token 3. Create a new FREE child box 4. POST `/api/v1/master-cartons/pack` with body `{"master_carton_id":"<closed_carton_id>","child_box_barcodes":["<new_barcode>"]}` 5. Check response | HTTP 400; response body contains error such as `"Cannot pack boxes into a closed carton"` or `"Carton is not in ACTIVE state"`; new child box remains FREE; closed carton `child_count` unchanged | API |
| TC-STATE-018 | Supervisor | Empty carton (0 child boxes) close — check behavior | P1 | 1. Login as Warehouse Operator, create a new master carton (status=CREATED, 0 child boxes) 2. Login as Supervisor, obtain token 3. POST `/api/v1/master-cartons/<empty_carton_id>/close` with `Authorization: Bearer <supervisor_token>` 4. Check response status | HTTP 400 if business rule prevents closing an empty carton (preferred behavior: `"Cannot close an empty carton"`); OR HTTP 200 if allowed. Either case: no 500 error; consistent behavior; carton must not move to DISPATCHED from CREATED state | API |

#### 53.3 — Concurrent Operations

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-CONC-001 | Warehouse Operator | Two users attempt to pack same child box simultaneously | P1 | 1. Obtain two separate Warehouse Operator tokens (two sessions) 2. Create 1 child box (status=FREE) and 2 master cartons (A and B) 3. Simultaneously fire two POST `/api/v1/master-cartons/pack` requests — one to pack into Carton A, one to pack into Carton B — using the same child box barcode 4. Inspect both responses and check final DB state of child box | Exactly one request succeeds (HTTP 200/201); the other request fails with HTTP 400 or 409 (conflict); the child box is packed into exactly one carton; no duplicate packing occurs; DB integrity maintained (no phantom PACKED state in two cartons) | API |
| TC-CONC-002 | Warehouse Operator + Dispatch Operator | User A unpacks while User B dispatches same carton simultaneously | P1 | 1. Set up a CLOSED master carton with 2 packed child boxes 2. Simultaneously fire: User A (Warehouse Operator) calls full-unpack endpoint on the carton; User B (Dispatch Operator) calls dispatch endpoint on the carton 3. Inspect both responses and final DB state | Exactly one operation succeeds; the other fails with HTTP 400 or 409; no partial state (e.g., carton is not half-unpacked and half-dispatched); child boxes end up consistently either all FREE or all DISPATCHED; no orphaned records | API |
| TC-CONC-003 | Warehouse Operator | Bulk create 500 child boxes — all have unique barcodes | P1 | 1. Login as Warehouse Operator, obtain token 2. POST `/api/v1/child-boxes/bulk` with body `{"product_id":"<valid_product_id>","count":500}` 3. Collect all returned barcodes 4. Check for uniqueness within the response array 5. Query DB to confirm all 500 barcodes are unique | HTTP 201; response array contains exactly 500 objects; all barcodes are unique within the response (no duplicates); DB UNIQUE constraint is not violated; all 500 rows inserted successfully | API |
| TC-CONC-004 | Warehouse Operator | Rapid sequential API calls — 10 creates in quick succession | P2 | 1. Login as Warehouse Operator, obtain token 2. Fire 10 sequential POST `/api/v1/child-boxes/bulk` requests with `count:1` each, one immediately after the other (no deliberate delay) 3. Check all 10 response statuses 4. Count total child boxes created | All 10 requests return HTTP 201; 10 distinct child boxes are created with 10 unique barcodes; no request results in a 500 or timeout; server handles sequential load without deadlock | API |
| TC-CONC-005 | Any | Login from two different sessions — both tokens valid | P1 | 1. POST `/api/v1/auth/login` with valid Admin credentials — capture `token_A` 2. POST `/api/v1/auth/login` again with the same Admin credentials — capture `token_B` 3. Use `token_A` to GET `/api/v1/auth/profile` 4. Use `token_B` to GET `/api/v1/auth/profile` | Both GET requests return HTTP 200; both tokens are valid simultaneously; both profile responses show the same user; system supports concurrent sessions per user | API |
| TC-CONC-006 | Any | Logout from one session — other session remains valid | P1 | 1. Login twice as Admin to get `token_A` and `token_B` 2. POST `/api/v1/auth/logout` with `Authorization: Bearer <token_A>` 3. Attempt GET `/api/v1/auth/profile` with `token_A` 4. Attempt GET `/api/v1/auth/profile` with `token_B` | `token_A` request returns HTTP 401 (invalidated); `token_B` request returns HTTP 200 (still valid); logout only invalidates the specific session, not all sessions for that user | API |

---

### Section 54: Error Handling & Recovery

#### 54.1 — API Error Responses

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-ERR-001 | Any | 404 endpoint returns JSON error — not HTML | P0 | 1. Login as Admin, obtain token 2. GET `/api/v1/nonexistentendpoint` with `Authorization: Bearer <admin_token>` 3. Inspect response `Content-Type` header and body | HTTP 404; `Content-Type: application/json`; response body is valid JSON (e.g., `{"message":"Not Found","statusCode":404}`); no HTML stack trace or Express default 404 page returned | API |
| TC-ERR-002 | Any | Invalid JSON body returns 400 with parse error | P0 | 1. POST `/api/v1/auth/login` with header `Content-Type: application/json` and raw body `{invalid json: unquoted}` 2. Check response status and body | HTTP 400; response body is valid JSON containing error about JSON parse failure such as `"Invalid JSON"` or `"Unexpected token"`; no 500 error | API |
| TC-ERR-003 | Any | Missing Content-Type header on POST — handled gracefully | P1 | 1. POST `/api/v1/auth/login` with NO `Content-Type` header and body `email=admin%40binny.com&password=Admin%40123` (form-encoded format) 2. Check response status | HTTP 400 (body not parsed as JSON) OR HTTP 415 Unsupported Media Type; response is valid JSON with an error message; no 500 server error; server does not crash | API |
| TC-ERR-004 | Any | Request body larger than 10MB — rejected | P1 | 1. Construct a POST body larger than 10MB (e.g., a JSON field with a 10MB+ string) 2. POST `/api/v1/products` with the oversized body and valid `Authorization` header 3. Check response | HTTP 413 Payload Too Large; response body is valid JSON with error message; server does not crash or hang; Express/body-parser `limit` configuration is enforced | API |
| TC-ERR-005 | Any | GET with very long query string — handled safely | P1 | 1. Construct a URL with a query string of 2001+ characters: GET `/api/v1/products?search=AAAAAA...` (repeat 'A' 2001 times) 2. Fire the request with valid `Authorization` header 3. Check response | HTTP 200 with empty or limited results, OR HTTP 414 URI Too Long, OR HTTP 400; server handles it without crashing; no 500 error; request does not hang | API |
| TC-ERR-006 | Any | Invalid UUID format in path parameter — returns 400 | P1 | 1. Login as Admin, obtain token 2. GET `/api/v1/products/not-a-valid-uuid` with `Authorization: Bearer <admin_token>` 3. Check response status and body | HTTP 400; response body contains error such as `"Invalid UUID format"` or `"Validation failed"`; server does not pass the malformed ID to the DB query (no DB error in response) | API |
| TC-ERR-007 | Any | Database connection lost — safe 500 error response | P2 | 1. Simulate a DB connection failure (e.g., temporarily stop the DB process or use a mock that throws a connection error) 2. Make a standard API call such as GET `/api/v1/products` with valid `Authorization` 3. Inspect response | HTTP 500; response body is valid JSON with a generic message such as `"Internal server error"` or `"Something went wrong"`; NO database connection details, passwords, stack traces, or file paths leaked in the response body | API |
| TC-ERR-008 | Any | Rate limit exceeded — 429 returned with retry info | P2 | 1. Fire 101+ requests to the same endpoint within 1 minute (beyond any configured rate limit) 2. Check the status of the request that exceeds the limit 3. Inspect response headers | HTTP 429 Too Many Requests; response body contains message such as `"Rate limit exceeded"`; response includes `Retry-After` header or equivalent; server continues to function for other clients after rate limit kicks in | API |

#### 54.2 — Frontend Error Handling

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-ERR-E2E-001 | Any | API timeout during page load — user sees error message | P1 | 1. Configure a network throttling tool (or Playwright route intercept) to delay the products API response by >30 seconds 2. Navigate to the Products page in the browser 3. Wait for the page to handle the timeout | A user-visible error message (toast or inline alert) appears saying something like `"Request timed out"` or `"Failed to load data"`; the page does NOT show a blank white screen; a retry button or navigation option is visible; no uncaught JS exception in console | E2E |
| TC-ERR-E2E-002 | Any | 500 server error — user sees "Something went wrong" toast | P1 | 1. Configure a Playwright route mock to return HTTP 500 with body `{"message":"Internal server error"}` for GET `/api/v1/products` 2. Navigate to the Products page 3. Observe UI behavior | A toast notification appears with text such as `"Something went wrong"` or the server error message; the page does not crash or show a blank screen; navigation away from the page still works; error is visible within 5 seconds | E2E |
| TC-ERR-E2E-003 | Any | Network disconnect during form submit — error shown, data not lost | P1 | 1. Navigate to Create Product form and fill in all required fields 2. Intercept the network with Playwright (`route.abort()`) to simulate a disconnect when the form is submitted 3. Click the Submit button 4. Observe UI | An error toast or inline message appears indicating network failure; the form fields RETAIN their entered values (data not lost on failed submit); the user can re-attempt after network is restored; no duplicate submission | E2E |
| TC-ERR-E2E-004 | Any | Navigate to non-existent route — 404 page or redirect | P1 | 1. Login as Admin to establish a valid session 2. Navigate browser directly to `http://localhost:3000/this-route-does-not-exist` 3. Observe what the browser renders | Either a custom 404 page is displayed with a message like `"Page not found"` and a link back to the dashboard; OR the router redirects to the dashboard (no blank page); no uncaught errors in console; no infinite redirect loop | E2E |
| TC-ERR-E2E-005 | Any | Token expired during active session — redirected to login | P0 | 1. Login as Admin to get a valid session token 2. Manually set the stored token to an expired JWT (past `exp` claim) in localStorage/SecureStore 3. Click any menu link or trigger an API call (e.g., navigate to Products page) 4. Observe routing behavior | Browser redirects to `http://localhost:3000/login`; no partial dashboard data is shown after redirect; a message may appear saying `"Session expired, please log in again"`; stored token is cleared from storage | E2E |
| TC-ERR-E2E-006 | Any | Multiple rapid clicks on Submit — only one request sent (debounce) | P1 | 1. Navigate to the Create Product form and fill in all required fields 2. Using Playwright, simulate 5 rapid clicks on the Submit button within 500ms 3. Monitor network requests (via Playwright's request interception or browser DevTools network log) | Exactly 1 POST request is sent to `/api/v1/products`; the submit button is disabled or shows a loading spinner after the first click (preventing duplicate clicks); no duplicate product is created in DB; no 429 rate limit triggered from duplicate client-side requests | E2E |

---

### Section 55: Pagination & Search

#### 55.1 — Pagination Tests

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-PAGE-001 | Admin | GET /products?page=1&limit=10 — returns correct structure | P0 | 1. Login as Admin, obtain token 2. Ensure at least 10 products exist in DB 3. GET `/api/v1/products?page=1&limit=10` with `Authorization: Bearer <admin_token>` 4. Inspect response body fields | HTTP 200; response contains `data` array with exactly 10 items; response contains `total` (total count of all products), `page: 1`, `limit: 10`, `totalPages` (= `Math.ceil(total/10)`); each item has `id`, `article_name`, `article_code`, `colour`, `size`, `mrp`, `category` | API |
| TC-PAGE-002 | Admin | GET /products?page=2&limit=10 — returns second page | P0 | 1. Login as Admin, obtain token 2. Ensure at least 11 products exist in DB 3. GET `/api/v1/products?page=2&limit=10` with `Authorization: Bearer <admin_token>` 4. Compare returned IDs with those from page 1 | HTTP 200; response `data` array contains the next set of products (different from page 1); `page: 2`; no duplicates with page 1 results; items are in consistent sort order (e.g., by `created_at` or `article_name`) | API |
| TC-PAGE-003 | Admin | GET /products?page=999 — returns empty data array | P0 | 1. Login as Admin, obtain token 2. GET `/api/v1/products?page=999&limit=10` with `Authorization: Bearer <admin_token>` 3. Check response status and body | HTTP 200; response `data` array is empty (`[]`); `page: 999`; `total` still reflects actual total count; `totalPages` is the real number of pages; no 404 or 500 error — empty page is a valid state | API |
| TC-PAGE-004 | Admin | GET /products?limit=0 — 400 or default applied | P1 | 1. Login as Admin, obtain token 2. GET `/api/v1/products?limit=0` with `Authorization: Bearer <admin_token>` 3. Check response | HTTP 400 with error `"limit must be at least 1"`; OR HTTP 200 with system applying a default limit (e.g., 10 or 20). Either way: no 500 error; if 200, `data` array is non-empty with the default limit applied | API |
| TC-PAGE-005 | Admin | GET /products?limit=101 — capped at max or rejected | P1 | 1. Login as Admin, obtain token 2. GET `/api/v1/products?limit=101` with `Authorization: Bearer <admin_token>` 3. Check response | HTTP 400 with error `"limit must not exceed 100"`; OR HTTP 200 with `limit` silently capped at 100 (response `data` has at most 100 items). Either way: no 500 error; no unbounded DB query returning thousands of rows | API |
| TC-PAGE-006 | Admin | GET /child-boxes with 50 items, limit=25 — totalPages equals 2 | P0 | 1. Login as Admin, obtain token 2. Ensure exactly 50 FREE child boxes exist for a known product (or create them) 3. GET `/api/v1/child-boxes?page=1&limit=25` with `Authorization: Bearer <admin_token>` 4. Inspect `totalPages` and `total` | HTTP 200; `total: 50`; `totalPages: 2`; `data` array has 25 items; `page: 1`; page 2 (GET with `page=2`) also returns 25 items | API |
| TC-PAGE-007 | Admin | GET /customers?search=Delhi — returns matching customers | P0 | 1. Login as Admin, obtain token 2. Ensure at least one customer has `address` or `firm_name` containing "Delhi" 3. GET `/api/v1/customers?search=Delhi` with `Authorization: Bearer <admin_token>` 4. Inspect returned customers | HTTP 200; all returned customers in `data` array contain "Delhi" (case-insensitive) in their searchable fields (firm_name or address); no customers without "Delhi" in searchable fields appear in results; `total` reflects match count | API |
| TC-PAGE-008 | Warehouse Operator | GET /child-boxes?search=BINNY-CB — search by barcode prefix | P0 | 1. Login as Warehouse Operator, obtain token 2. GET `/api/v1/child-boxes?search=BINNY-CB` with `Authorization: Bearer <warehouse_token>` 3. Inspect returned results | HTTP 200; all items in `data` have `barcode` starting with `"BINNY-CB"`; if the system has any child boxes they should all match (since all barcodes use this prefix); `total` is >= 0; no 500 error | API |

---

## Phase 14: Full Lifecycle End-to-End Tests

### Section 56: Complete Lifecycle — Multi-Role E2E

#### 56.1 — Lifecycle 1: Product → Child Box → Pack → Close → Dispatch → Trace

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-LIFE-001 | Multi-Role | Full lifecycle via API — create through trace | P0 | **Step 1 — Create Product (Admin):** Login as Admin. POST `/api/v1/products` with body `{"article_name":"TestArticle","article_code":"TART001","colour":"Black","size":"8","mrp":299,"category":"Gents","section_id":"<valid_section_id>"}`. Record returned `product_id` and `sku`. **Step 2 — Verify product:** GET `/api/v1/products/<product_id>`. Confirm `article_name="TestArticle"`, `colour="Black"`, `size="8"`, `mrp=299`, auto-generated `sku` is non-null. **Step 3 — Create 5 child boxes (Warehouse Operator):** Login as Warehouse Operator. POST `/api/v1/child-boxes/bulk` with body `{"product_id":"<product_id>","count":5}`. Record all 5 returned barcodes. **Step 4 — Verify child boxes:** Confirm response array has 5 items; all have `status: "FREE"`; all barcodes start with `"BINNY-CB-"`; all `product_id` fields match. **Step 5 — Create master carton with 3 boxes (Warehouse Operator):** POST `/api/v1/master-cartons` to get a `carton_id`. Then POST `/api/v1/master-cartons/pack` with body `{"master_carton_id":"<carton_id>","child_box_barcodes":["<barcode1>","<barcode2>","<barcode3>"]}`. **Step 6 — Verify carton:** GET `/api/v1/master-cartons/<carton_id>`. Confirm `status: "ACTIVE"`, `child_count: 3`. **Step 7 — Verify 3 boxes are PACKED:** GET each of the 3 packed child boxes. Confirm `status: "PACKED"`. **Step 8 — Verify 2 boxes remain FREE:** GET the 2 remaining child boxes. Confirm `status: "FREE"`. **Step 9 — Close carton (Supervisor):** Login as Supervisor. POST `/api/v1/master-cartons/<carton_id>/close`. **Step 10 — Verify carton CLOSED:** GET carton. Confirm `status: "CLOSED"`. **Step 11 — Create dispatch (Dispatch Operator):** Login as Dispatch Operator. POST `/api/v1/dispatch` with body `{"master_carton_ids":["<carton_id>"],"customer_id":"<valid_customer_id>"}`. Record `dispatch_id`. **Step 12 — Verify dispatch:** GET `/api/v1/dispatch/<dispatch_id>`. Confirm carton `status: "DISPATCHED"`, dispatch record exists with `dispatched_at` timestamp, `customer_id` matches. **Step 13 — Verify 3 boxes DISPATCHED:** GET each of the 3 previously PACKED child boxes. Confirm all have `status: "DISPATCHED"`. **Step 14 — Trace a dispatched box (any role):** Login as Admin. GET `/api/v1/trace/<barcode_of_dispatched_box>`. **Step 15 — Verify trace timeline:** Inspect trace response. | All steps return expected HTTP status codes (201/200). Final trace response contains: `childBox` object with `status: "DISPATCHED"`, `masterCarton` object with `status: "DISPATCHED"`, `dispatch` object with customer and timestamp details, `timeline` array with events in chronological order: `CHILD_CREATED` → `CHILD_PACKED` → `CARTON_CLOSED` → `CARTON_DISPATCHED` → `CHILD_DISPATCHED`. Two unpacked boxes retain `status: "FREE"` throughout. No intermediate state errors. | API |

#### 56.2 — Lifecycle 2: Pack → Unpack → Repack

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-LIFE-002 | Multi-Role | Repack lifecycle via API — carton A to carton B | P1 | **Step 1 — Create 4 child boxes:** Login as Warehouse Operator. POST `/api/v1/child-boxes/bulk` with `count:4`. Record barcodes B1, B2, B3, B4. **Step 2 — Create Carton A with B1 and B2:** POST `/api/v1/master-cartons` → `carton_A_id`. POST `/api/v1/master-cartons/pack` with barcodes [B1, B2]. Verify Carton A: `status: "ACTIVE"`, `child_count: 2`. **Step 3 — Create Carton B with B3 and B4:** POST `/api/v1/master-cartons` → `carton_B_id`. POST `/api/v1/master-cartons/pack` with barcodes [B3, B4]. Verify Carton B: `status: "ACTIVE"`, `child_count: 2`. **Step 4 — Repack B1 from Carton A to Carton B:** POST `/api/v1/master-cartons/unpack-box` or equivalent endpoint with body `{"child_box_barcode":"<B1>"}`. Then POST `/api/v1/master-cartons/pack` with body `{"master_carton_id":"<carton_B_id>","child_box_barcodes":["<B1>"]}`. **Step 5 — Verify carton counts:** GET Carton A — confirm `child_count: 1`, contains only B2. GET Carton B — confirm `child_count: 3`, contains B1, B3, B4. **Step 6 — Full unpack Carton A:** POST `/api/v1/master-cartons/<carton_A_id>/full-unpack`. **Step 7 — Verify Carton A empty and B2 FREE:** GET Carton A — `child_count: 0`. GET B2 — `status: "FREE"`, `master_carton_id: null`. **Step 8 — Trace the repacked box B1:** GET `/api/v1/trace/<B1_barcode>`. Inspect timeline. | Steps 1-5 complete without error. Carton A has exactly 1 box (B2) after repack. Carton B has exactly 3 boxes (B1, B3, B4). After full unpack of Carton A: Carton A `child_count: 0`; B2 status is `FREE`. Trace of B1 timeline shows events: `CHILD_CREATED` → `CHILD_PACKED` (into Carton A) → `CHILD_REPACKED` or equivalent event showing move from Carton A to Carton B. No duplicate records. No orphaned master_carton references. | API |

#### 56.3 — Lifecycle 3: Multi-Size Batch → Pack → Dispatch

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-LIFE-003 | Multi-Role | Multi-size lifecycle via API — 4 sizes in one carton | P1 | **Step 1 — Create 4 products (same article, same colour, 4 sizes):** Login as Admin. POST `/api/v1/products` four times: `{article_name:"TestArticle",article_code:"TART002",colour:"Black",size:"6",...}`, then size 7, 8, 9. Record `product_id_s6`, `product_id_s7`, `product_id_s8`, `product_id_s9`. **Step 2 — Bulk multi-size generate:** Login as Warehouse Operator. POST `/api/v1/child-boxes/bulk-multi-size` (or equivalent) with body `[{"product_id":"<id_s6>","count":3},{"product_id":"<id_s7>","count":2},{"product_id":"<id_s8>","count":4},{"product_id":"<id_s9>","count":1}]`. Record all 10 returned barcodes with their sizes. **Step 3 — Verify 10 boxes:** Confirm response array has 10 items; 3 with size 6, 2 with size 7, 4 with size 8, 1 with size 9; all `status: "FREE"`. **Step 4 — Create master carton with all 10:** POST `/api/v1/master-cartons` → `carton_id`. POST `/api/v1/master-cartons/pack` with all 10 barcodes. **Step 5 — Verify carton assortment:** GET `/api/v1/master-cartons/<carton_id>/children` or equivalent. Confirm: `child_count: 10`; assortment breakdown: size 6 = 3, size 7 = 2, size 8 = 4, size 9 = 1. **Step 6 — Close and dispatch:** Supervisor closes carton. Dispatch Operator creates dispatch with the carton. **Step 7 — Verify inventory update:** GET `/api/v1/inventory` or inventory report. Verify: 10 pairs dispatched for TestArticle/Black across sizes 6/7/8/9; stock counters updated correctly. | All 10 boxes created with correct sizes. Carton assortment breakdown matches (3+2+4+1=10). Close and dispatch succeed. Inventory report reflects: 10 pairs dispatched; breakdown by size is accurate; no double-counting. All 10 child boxes show `status: "DISPATCHED"`. | API |

#### 56.4 — Lifecycle 4: Customer Hierarchy → Dispatch

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-LIFE-004 | Admin + Dispatch Operator | Customer hierarchy dispatch lifecycle | P1 | **Step 1 — Create Primary Dealer:** Login as Admin. POST `/api/v1/customers` with body `{"firm_name":"TestDealer Pvt Ltd","address":"123 Main St, Delhi","mobile":"9876543210","gstin":"22AAAAA0000A1Z5","customer_type":"Primary Dealer"}`. Record `primary_customer_id`. **Step 2 — Create Sub Dealer linked to primary:** POST `/api/v1/customers` with body `{"firm_name":"TestDealer Sub Branch","address":"456 Sub St, Delhi","mobile":"9876543211","customer_type":"Sub Dealer","parent_customer_id":"<primary_customer_id>"}`. Verify response: `parent_customer_id` matches; address may be auto-filled from primary. Record `sub_customer_id`. **Step 3 — Create and close a master carton:** Create child boxes → pack into carton → Supervisor closes carton. Record `carton_id`. **Step 4 — Dispatch to Sub Dealer:** Login as Dispatch Operator. POST `/api/v1/dispatch` with body `{"master_carton_ids":["<carton_id>"],"customer_id":"<sub_customer_id>"}`. Record `dispatch_id`. **Step 5 — Verify dispatch record:** GET `/api/v1/dispatch/<dispatch_id>`. Inspect `customer_id`, `customer_name`, `address` in response. **Step 6 — Check dispatch report:** GET `/api/v1/reports/dispatch` or equivalent. Confirm the dispatch appears in report grouped under the customer. | Sub Dealer creation succeeds with `parent_customer_id` linked. Dispatch to Sub Dealer succeeds (HTTP 201). Dispatch record shows `customer_id: <sub_customer_id>`, `destination` auto-filled from customer's address. Report groups dispatch under correct customer (firm_name matches). Primary-Sub relationship is reflected in the dispatch hierarchy view. | API |

#### 56.5 — Lifecycle 5: Playwright Full E2E (browser-based)

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-LIFE-E2E-001 | Multi-Role | Full lifecycle in browser via Playwright | P0 | **Step 1 — Login as Admin:** Navigate to `http://localhost:3000/login`. Enter Admin email and password. Click Sign In. Verify redirect to `/dashboard`. **Step 2 — Create a product:** Navigate to `/products`. Click "Add Product". Fill in: Article Name = "E2ETestArticle", Article Code = "E2EART01", Colour = "Navy", Size = "8", MRP = "399", Category = "Gents", Section = select first available. Click Save. Wait for success toast. Verify product appears in product list. **Step 3 — Generate child box labels:** Navigate to `/child-boxes/generate`. Select the newly created product from dropdown. Set count = 3. Click "Generate Labels". Wait for PDF/label preview or success state. Verify 3 new child boxes appear in `/child-boxes` list with status FREE. Record 3 barcodes (CB1, CB2, CB3). **Step 4 — Create master carton:** Navigate to `/master-cartons`. Click "Create Carton". Enter barcodes CB1, CB2, CB3 (one by one or bulk scan). Click "Create" / "Pack". Verify success toast. Verify carton appears in list. Navigate to carton detail page. Verify assortment shows size 8 = 3. **Step 5 — Navigate to carton detail:** Navigate to `/master-cartons/<carton_id>`. Verify `child_count: 3`, status = ACTIVE, assortment table shows 3 pairs size 8 Navy E2ETestArticle. **Step 6 — Storage/Seal:** Navigate to `/storage` (or carton detail seal action). Find or scan the carton barcode. Click Seal/Close. Verify carton status changes to CLOSED. **Step 7 — Dispatch:** Navigate to `/dispatch`. Click "New Dispatch". Scan or enter carton barcode. Select a customer from dropdown. Click "Dispatch". Verify success state. Verify carton status is now DISPATCHED. **Step 8 — Scan & Trace:** Navigate to `/scan-trace`. Enter barcode CB1 in the search/scan field. Wait for trace result. Inspect displayed timeline events. **Step 9 — Reports:** Navigate to `/reports/dispatch`. Verify the dispatch record appears with correct customer, carton barcode, and item count. **Step 10 — Inventory:** Navigate to `/inventory`. Verify E2ETestArticle stock reflects the dispatch (items moved from stock to dispatched). | Each navigation step loads the correct page without error. Product appears in list after creation. 3 child boxes created with FREE status and valid barcodes. Carton created successfully with ACTIVE status and assortment count=3. Seal action transitions carton to CLOSED. Dispatch creates a record and transitions carton to DISPATCHED. Trace shows timeline with at minimum: CHILD_CREATED, CHILD_PACKED, CARTON_CLOSED, CARTON_DISPATCHED, CHILD_DISPATCHED. Reports page shows the dispatch row. Inventory reflects updated stock numbers. No console errors or unhandled exceptions throughout the flow. | E2E |

#### 56.6 — Lifecycle 6: Role Handoff E2E

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-LIFE-E2E-002 | Multi-Role | Multi-role workflow — full handoff in browser | P0 | **Step 1 — Admin creates product and customer:** Login as Admin. Create product: "RoleHandoffArticle" / "Black" / size "9" / MRP 499. Create customer: "RoleHandoff Customer". Logout. **Step 2 — Warehouse Operator generates and packs:** Login as Warehouse Operator. Navigate to `/child-boxes/generate`. Select "RoleHandoffArticle/Black/size 9". Generate 2 boxes. Note barcodes (WB1, WB2). Navigate to `/master-cartons`. Create new carton. Pack WB1 and WB2 into it. Verify carton is ACTIVE with 2 boxes. Logout. **Step 3 — Supervisor seals carton:** Login as Supervisor. Navigate to `/storage` or `/master-cartons`. Locate the carton (created by Warehouse Operator). Click Seal/Close. Verify carton status = CLOSED. Logout. **Step 4 — Dispatch Operator dispatches:** Login as Dispatch Operator. Navigate to `/dispatch`. Create new dispatch. Select or scan the CLOSED carton. Select "RoleHandoff Customer". Confirm dispatch. Verify carton is now DISPATCHED. Logout. **Step 5 — Admin verifies end state:** Login as Admin. Navigate to `/reports/dispatch`. Verify dispatch record shows: carton barcode, "RoleHandoff Customer", 2 pairs dispatched. Navigate to `/scan-trace`. Enter barcode WB1. Verify timeline shows all events across all roles with correct timestamps. | Step 1: Admin can create product and customer without issue. Step 2: Warehouse Operator can see the product in dropdown, generate boxes, create and pack carton — supervisor-only seal not available to Warehouse Operator. Step 3: Supervisor can see the carton and successfully close it — dispatch not available to Supervisor. Step 4: Dispatch Operator can dispatch the CLOSED carton — cannot create cartons or generate boxes. Step 5: Admin sees full audit trail. Trace timeline shows events from all 4 roles in sequence: CHILD_CREATED (Warehouse Op) → CHILD_PACKED (Warehouse Op) → CARTON_CLOSED (Supervisor) → CARTON_DISPATCHED (Dispatch Op) → CHILD_DISPATCHED. All role restrictions enforced throughout — no role can perform another role's exclusive action. | E2E |

---

### Section 57: Regression Tests

#### 57.1 — Previously Fixed Bugs

| TC ID | Role | Title | Priority | Steps | Expected Result | Type |
|-------|------|-------|----------|-------|-----------------|------|
| TC-REG-001 | Any | Traceability API for master carton barcode does not crash | P0 | 1. Login as Admin, obtain token 2. Obtain a valid master carton barcode (e.g., `BINNY-MC-0001`) from the DB or a previous create call 3. GET `/api/v1/trace/<master_carton_barcode>` with `Authorization: Bearer <admin_token>` 4. Inspect response status and body | HTTP 200 with carton trace data; OR HTTP 404 if barcode not found — but NO HTTP 500; no server crash; response is valid JSON; the column collision bug (where `id` or `barcode` alias clashed between child_box and master_carton table joins) does not occur | API |
| TC-REG-002 | Warehouse Operator | Child box label shows Article No correctly (not "undefined") | P0 | 1. Login as Warehouse Operator, obtain token 2. POST `/api/v1/child-boxes/bulk` with a valid `product_id` (product has `article_code = "TART001"`) 3. POST `/api/v1/child-boxes/labels` or equivalent label generation endpoint with the new child box IDs 4. Inspect the API response or generated PDF label content | The label response or PDF contains `article_code: "TART001"` (not the string `"undefined"`); the child box API response includes `article_code` field from the joined products table; `article_code` is present in every child box object returned | API |
| TC-REG-003 | Warehouse Operator | Child box label shows MRP correctly (not "NaN") | P0 | 1. Login as Warehouse Operator, obtain token 2. Create child boxes for a product with `mrp: 299` 3. Retrieve child box details via GET `/api/v1/child-boxes/<id>` 4. Generate label or inspect the child box API response | Response contains `mrp: 299` (numeric value, not `null` or missing); label generation returns MRP as `"₹299.00"` or equivalent formatted string — not `"NaN"` or `"₹NaN"`; the `mrp` column is correctly included in the child box SELECT query via product join | API |
| TC-REG-004 | Admin | Master carton detail shows MRP correctly (not "₹NaN") | P0 | 1. Login as Admin, obtain token 2. Create a master carton with child boxes packed into it (boxes linked to a product with `mrp: 349`) 3. GET `/api/v1/master-cartons/<carton_id>/children` or the carton detail endpoint 4. Inspect each child box object's `mrp` field | Each child box in the carton detail response has `mrp: 349` (numeric); frontend renders `"₹349.00"` — not `"₹NaN"` or `"₹undefined"`; the `getCartonChildren` DB query includes `mrp` in the SELECT via products join | API |
| TC-REG-005 | Any | Product list shows article_name correctly (not blank) | P0 | 1. Login as Admin, obtain token 2. GET `/api/v1/products?page=1&limit=20` with `Authorization: Bearer <admin_token>` 3. Inspect all items in `data` array | Every product object in the response has a non-empty `article_name` string; no item has `article_name: null`, `article_name: ""`, or `article_name: undefined`; the SQL column alias for `article_name` is correctly set in the products query | API |
| TC-REG-006 | Dispatch Operator | Primary dealer dropdown shows active dealers | P0 | 1. Login as Dispatch Operator, obtain token 2. Ensure at least one customer with `customer_type: "Primary Dealer"` and `is_active: true` exists in DB 3. Ensure another customer exists with `is_active: false` 4. GET `/api/v1/customers?customer_type=Primary Dealer` (or the dropdown data endpoint) | Response contains only active primary dealers (`is_active: true`); the inactive dealer does NOT appear in the list; dropdown in frontend shows the correct dealers; `is_active=true` filter is applied server-side | API |
| TC-REG-007 | Any | Login works after server restart — autoSeed password valid | P1 | 1. Restart the backend server (simulate with `docker-compose restart backend` or equivalent) 2. Wait for server to fully start (health check passes) 3. POST `/api/v1/auth/login` with body `{"email":"admin@binny.com","password":"Admin@123"}` 4. Check response | HTTP 200 with valid token; `user.role: "Admin"`; the autoSeed mechanism (which seeds the default admin on first start) uses a correctly hashed password that matches `"Admin@123"`; no `"Invalid credentials"` error after fresh start when DB has seeded data | API |
| TC-REG-008 | Any | Paginated API response has pagination/meta field — interceptor compatibility | P0 | 1. Login as Admin, obtain token 2. GET `/api/v1/products?page=1&limit=5` with `Authorization: Bearer <admin_token>` 3. Inspect the raw API response structure 4. Verify the frontend axios interceptor correctly unwraps the envelope | API response contains either `{data:[...], pagination:{page,limit,total,totalPages}}` OR `{data:[...], meta:{page,limit,total,totalPages}}`; the frontend axios response interceptor handles BOTH formats (unwraps `pagination` or `meta`); product list renders with correct data; no `undefined.map is not a function` error in console | API + Integration |

---

## Execution Phase Summary

| Phase | Sections | Est. Test Cases | Playwright Specs | Priority |
|-------|----------|----------------|-----------------|----------|
| Phase 1 | 32-33 | ~57 | 16-rbac.spec.ts | Critical |
| Phase 2 | 34-35 | ~48 | 17-products-rbac.spec.ts | Critical |
| Phase 3 | 36 | ~32 | 18-customers-rbac.spec.ts | High |
| Phase 4 | 37 | ~38 | 19-childbox-rbac.spec.ts | Critical |
| Phase 5 | 38 | ~46 | 20-cartons-rbac.spec.ts | Critical |
| Phase 6 | 39 | ~30 | 21-dispatch-rbac.spec.ts | High |
| Phase 7 | 40 | ~28 | 22-scan-trace.spec.ts | High |
| Phase 8 | 41 | ~32 | 23-inventory-dash.spec.ts | High |
| Phase 9 | 42 | ~32 | 24-reports-rbac.spec.ts | Medium |
| Phase 10 | 43 | ~32 | 25-users-rbac.spec.ts | High |
| Phase 11 | 44-45 | ~44 | 26-ui-pwa.spec.ts | Medium |
| Phase 12 | 46-51 | ~54 | Manual (mobile) | High |
| Phase 13 | 52-55 | ~60 | 27-edge-cases.spec.ts | Medium |
| Phase 14 | 56-57 | ~14 | 28-lifecycle.spec.ts | Critical |
| **TOTAL** | **32-57** | **~547** | **13 new spec files** | |

---

### Phase 13–14 Detailed Test Count

| Section | Title | API Tests | E2E Tests | Total |
|---------|-------|-----------|-----------|-------|
| 52.1 | String Boundary | 8 | 0 | 8 |
| 52.2 | Numeric Boundary | 6 | 0 | 6 |
| 52.3 | Special Characters & Injection | 7 | 1 | 8 |
| 53.1 | Child Box State Transitions | 8 | 0 | 8 |
| 53.2 | Master Carton State Transitions | 10 | 0 | 10 |
| 53.3 | Concurrent Operations | 6 | 0 | 6 |
| 54.1 | API Error Responses | 8 | 0 | 8 |
| 54.2 | Frontend Error Handling | 0 | 6 | 6 |
| 55.1 | Pagination & Search | 8 | 0 | 8 |
| 56.1 | Full Lifecycle API | 1 | 0 | 1 |
| 56.2 | Repack Lifecycle | 1 | 0 | 1 |
| 56.3 | Multi-Size Lifecycle | 1 | 0 | 1 |
| 56.4 | Customer Hierarchy Lifecycle | 1 | 0 | 1 |
| 56.5 | Full E2E Browser Lifecycle | 0 | 1 | 1 |
| 56.6 | Role Handoff E2E | 0 | 1 | 1 |
| 57.1 | Regression Tests | 7 | 1 | 8 |
| **Total** | | **72** | **10** | **82** |

### Recommended Playwright Spec File Structure

```
tests/
├── 27-edge-cases.spec.ts       # Phase 13: Sections 52-55
│   ├── describe('Input Validation')     → TC-EDGE-001 to TC-EDGE-022
│   ├── describe('State Machine')        → TC-STATE-001 to TC-STATE-018
│   ├── describe('Error Handling API')   → TC-ERR-001 to TC-ERR-008
│   ├── describe('Error Handling E2E')   → TC-ERR-E2E-001 to TC-ERR-E2E-006
│   └── describe('Pagination Search')    → TC-PAGE-001 to TC-PAGE-008
└── 28-lifecycle.spec.ts        # Phase 14: Sections 56-57
    ├── describe('Lifecycle API Tests')  → TC-LIFE-001 to TC-LIFE-004
    ├── describe('Lifecycle E2E')        → TC-LIFE-E2E-001 to TC-LIFE-E2E-002
    └── describe('Regression Tests')    → TC-REG-001 to TC-REG-008
```

### Priority Execution Order

| Priority | Test IDs | When to Run |
|----------|----------|-------------|
| P0 — Smoke | TC-STATE-001, TC-STATE-002, TC-STATE-003, TC-STATE-008, TC-STATE-009, TC-STATE-010, TC-STATE-011, TC-EDGE-017, TC-EDGE-022, TC-ERR-001, TC-ERR-002, TC-REG-001, TC-REG-002, TC-REG-003, TC-LIFE-001 | Every CI build |
| P1 — Full | All TC-EDGE, TC-STATE, TC-CONC, TC-ERR, TC-PAGE, TC-REG tests | Nightly or pre-release |
| P2 — Extended | TC-ERR-007, TC-ERR-008, TC-CONC-004, TC-LIFE-E2E-001, TC-LIFE-E2E-002 | Pre-production release |
