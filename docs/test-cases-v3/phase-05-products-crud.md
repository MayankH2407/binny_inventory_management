# Phase 05 — Products: CRUD & Image Upload

**System:** Binny Footwear Inventory Management System (Mahavir Polymers Pvt. Ltd.)
**Suite version:** v3
**Phase:** 05 of 20
**API base:** `http://localhost:5000/api/v1` (local) / `https://srv1409601.hstgr.cloud/binny/api/v1` (portal)
**Frontend base:** `http://localhost:3000` (local) / `https://srv1409601.hstgr.cloud/binny/` (portal)
**Last updated:** 2026-04-30

---

## Shared test fixtures

The following data must exist before running this phase. Create them from Phase 03 (sections) if absent.

| Fixture alias | Value |
|---|---|
| `SECTION_ID_A` | Section named `"Hawaii"` — UUID obtained from `GET /sections` |
| `SECTION_ID_B` | Section named `"Merlin"` — UUID obtained from `GET /sections` |
| `PRODUCT_UUID_A` | Created by TC-PROD-001 — `article_name="Busker"`, `colour="White"`, `size="6"`, `mrp=299`, `category="Gents"`, `section="Hawaii"` |
| `INACTIVE_PRODUCT_UUID` | Created by TC-PROD-027 and then deactivated |

---

## Phase 05: Products CRUD & Image Upload

### Section 1: Create Product (POST /products)

#### 1.1 — Role-based creation

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-001 | Admin | Admin creates product — all required fields | P0 | 1. `POST /api/v1/auth/login` as `admin@binny.com` / `Admin@123`, save JWT. 2. `POST /api/v1/products` with header `Authorization: Bearer <JWT>` and body `{"article_name":"Busker","article_code":"BSK-001","colour":"White","size":"6","mrp":299,"category":"Gents","section":"Hawaii"}`. | HTTP 201. Response: `{"success":true,"data":{"id":"<uuid>","sku":"HAWAII-BUSKER-GENTS-01-WHITE","article_name":"Busker","article_code":"BSK-001","colour":"White","size":"6","mrp":"299.00","category":"Gents","section":"Hawaii","is_active":true}}`. `sku` matches pattern `HAWAII-BUSKER-GENTS-NN-WHITE`. `id` is a valid UUID. Save `id` as `PRODUCT_UUID_A`. | API | SKU generated by `skuGenerator.ts` — format: `<SECTION>-<ARTICLE>-<CATEGORY>-<NN>-<COLOUR>` |
| TC-PROD-002 | Admin | Admin creates product — all optional fields included | P1 | 1. Login as Admin. 2. `POST /api/v1/products` body: `{"article_name":"Busker","article_code":"BSK-001","colour":"Black","size":"7","mrp":299,"category":"Gents","section":"Hawaii","description":"Summer sandal","location":"VKIA","article_group":"Casual","hsn_code":"64039900"}`. | HTTP 201. `description`, `location`, `article_group`, `hsn_code` all present in response `data`. `location = "VKIA"`. `sku` is non-empty. | API | |
| TC-PROD-003 | Supervisor | Supervisor creates product successfully | P1 | 1. Login as `supervisor@binny.com` / `Sup@123`. 2. `POST /api/v1/products` body same as TC-PROD-001 but `colour="Red"`, `size="8"`. | HTTP 201. Valid `id` and `sku` returned. `is_active = true`. | API | |
| TC-PROD-004 | Warehouse Operator | Warehouse Operator cannot create product | P0 | 1. Login as `warehouse@binny.com` / `Wh@123`. 2. `POST /api/v1/products` body: `{"article_name":"X","article_code":"X-001","colour":"Blue","size":"6","mrp":100,"category":"Gents","section":"Hawaii"}`. | HTTP 403. Response body contains `"error"` or `"message"` indicating forbidden. No row inserted in `products` table. | API | |
| TC-PROD-005 | Dispatch Operator | Dispatch Operator cannot create product | P0 | 1. Login as `dispatch@binny.com` / `Dp@123`. 2. `POST /api/v1/products` same body as TC-PROD-004. | HTTP 403. Forbidden. No product created. | API | |
| TC-PROD-006 | Any | Unauthenticated create returns 401 | P0 | 1. `POST /api/v1/products` with no `Authorization` header, body same as TC-PROD-001. | HTTP 401. Response contains authentication error. No product created. | API | |

---

#### 1.2 — Validation: missing required fields

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-007 | Admin | Missing article_name returns 400 | P0 | 1. Login as Admin. 2. `POST /api/v1/products` body: `{"article_code":"BSK-001","colour":"White","size":"6","mrp":299,"category":"Gents","section":"Hawaii"}` (no `article_name`). | HTTP 400. Validation error referencing `article_name`. No product created. | API | |
| TC-PROD-008 | Admin | Missing colour returns 400 | P0 | 1. Login as Admin. 2. `POST /api/v1/products` body omits `colour`. | HTTP 400. Error references `colour`. No product created. | API | |
| TC-PROD-009 | Admin | Missing size returns 400 | P0 | 1. Login as Admin. 2. `POST /api/v1/products` body omits `size`. | HTTP 400. Error references `size`. No product created. | API | |
| TC-PROD-010 | Admin | Missing mrp returns 400 | P0 | 1. Login as Admin. 2. `POST /api/v1/products` body omits `mrp`. | HTTP 400. Error references `mrp`. | API | |
| TC-PROD-011 | Admin | Missing category returns 400 | P0 | 1. Login as Admin. 2. `POST /api/v1/products` body omits `category`. | HTTP 400. Error references `category`. | API | |
| TC-PROD-012 | Admin | Missing section returns 400 | P0 | 1. Login as Admin. 2. `POST /api/v1/products` body omits `section`. | HTTP 400. Error references `section`. | API | |

---

#### 1.3 — Validation: wrong-type / boundary

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-013 | Admin | Negative MRP returns 400 | P0 | 1. Login as Admin. 2. `POST /api/v1/products` body with `mrp: -1`. | HTTP 400. Error: `"MRP must be positive"`. | API | |
| TC-PROD-014 | Admin | Zero MRP returns 400 | P1 | 1. Login as Admin. 2. `POST /api/v1/products` body with `mrp: 0`. | HTTP 400. MRP positive validation fails. | API | |
| TC-PROD-015 | Admin | Invalid category value returns 400 | P0 | 1. Login as Admin. 2. `POST /api/v1/products` body with `category: "Kids"` (not in enum). | HTTP 400. Error: `"Category must be one of: Gents, Ladies, Boys, Girls"`. | API | Valid values: `Gents`, `Ladies`, `Boys`, `Girls` |
| TC-PROD-016 | Admin | Invalid location value returns 400 | P1 | 1. Login as Admin. 2. `POST /api/v1/products` body with `location: "XYZ"`. | HTTP 400. Error: `"Location must be one of: VKIA, MIA, F540"`. | API | Valid values: `VKIA`, `MIA`, `F540` |
| TC-PROD-017 | Admin | article_name too short (1 char) returns 400 | P1 | 1. Login as Admin. 2. `POST /api/v1/products` body with `article_name:"X"`. | HTTP 400. Error: `"Article name must be at least 2 characters"`. | API | |
| TC-PROD-018 | Admin | article_name at max length (150 chars) succeeds | P2 | 1. Login as Admin. 2. `POST /api/v1/products` body with `article_name` = string of exactly 150 characters, all other fields valid. | HTTP 201. Product created. `article_name` truncated to 150 chars exactly. | API | |
| TC-PROD-019 | Admin | article_name exceeds 150 chars returns 400 | P1 | 1. Login as Admin. 2. `POST /api/v1/products` body with `article_name` = 151-character string. | HTTP 400. Error: `"Article name must not exceed 150 characters"`. | API | |
| TC-PROD-020 | Admin | MRP exceeds max value returns 400 | P2 | 1. Login as Admin. 2. `POST /api/v1/products` body with `mrp: 100000000`. | HTTP 400. Error referencing MRP max. | API | Max is 99999999.99 |

---

#### 1.4 — SKU uniqueness

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-021 | Admin | SKU serial increments for same article+colour | P1 | 1. Login as Admin. 2. Create product A: `article_name="Busker"`, `colour="White"`, `size="9"`, `category="Gents"`, `section="Hawaii"`. Note SKU serial. 3. Create product B: same article+colour+category+section but `size="10"`. | Product A has SKU ending `-01-WHITE` (or next serial if records exist). Product B has SKU ending `-02-WHITE` (one higher). Both products are distinct rows with unique `id`. | API | Serial is count-based; if 01 and 02 already exist they will be 03 and 04 |
| TC-PROD-022 | Admin | Duplicate (article_name, colour, size) returns 409 | P1 | 1. Login as Admin. Pre-condition: `PRODUCT_UUID_A` exists with `article_name="Busker"`, `colour="White"`, `size="6"`. 2. `POST /api/v1/products` body: `{"article_name":"Busker","article_code":"BSK-001","colour":"White","size":"6","mrp":299,"category":"Gents","section":"Hawaii"}`. | HTTP 409 or 400 (unique constraint violation). Response body error message indicating duplicate product. No second row created for `(Busker, White, 6)`. | API | Uniqueness enforced at DB level on `(article_name, colour, size)` |

---

### Section 2: Get Products (GET /products)

#### 2.1 — List & pagination

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-023 | Any (Admin) | GET /products returns paginated list | P0 | 1. Login as Admin. 2. `GET /api/v1/products`. | HTTP 200. Response: `{"success":true,"data":[...],"total":<n>,"page":1,"limit":25,"totalPages":<n>}`. Each item contains `id`, `sku`, `article_name`, `article_code`, `colour`, `size`, `mrp`, `category`, `section`, `is_active`, `created_at`. | API | |
| TC-PROD-024 | Any | GET /products?page=2&limit=10 returns correct slice | P1 | 1. Pre-condition: At least 11 products exist. 2. Login as Admin. 3. `GET /api/v1/products?page=2&limit=10`. | HTTP 200. `data` array has at most 10 items. `page = 2`. `limit = 10`. Items are the 11th–20th products ordered by `created_at DESC`. | API | |
| TC-PROD-025 | Warehouse Operator | Warehouse Operator can read product list | P1 | 1. Login as `warehouse@binny.com`. 2. `GET /api/v1/products`. | HTTP 200. Valid paginated list returned. Same structure as TC-PROD-023. | API | All authenticated roles can read |
| TC-PROD-026 | Dispatch Operator | Dispatch Operator can read product list | P1 | 1. Login as `dispatch@binny.com`. 2. `GET /api/v1/products`. | HTTP 200. Valid response. | API | |

---

#### 2.2 — Filters

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-027 | Admin | Filter by is_active=true returns only active | P1 | 1. Login as Admin. 2. Create a product and deactivate it (`PUT /api/v1/products/<id>` `{"is_active":false}`). Save its id as `INACTIVE_PRODUCT_UUID`. 3. `GET /api/v1/products?is_active=true`. | HTTP 200. All items in `data` have `is_active = true`. `INACTIVE_PRODUCT_UUID` is not present. | API | |
| TC-PROD-028 | Admin | Filter by is_active=false returns only inactive | P1 | 1. Pre-condition: `INACTIVE_PRODUCT_UUID` exists. 2. `GET /api/v1/products?is_active=false`. | HTTP 200. All items in `data` have `is_active = false`. `INACTIVE_PRODUCT_UUID` is present. | API | |
| TC-PROD-029 | Admin | Filter by section returns matching products | P1 | 1. Login as Admin. 2. `GET /api/v1/products?section=Hawaii`. | HTTP 200. All items in `data` have `section = "Hawaii"`. No items from other sections appear. | API | |
| TC-PROD-030 | Admin | Filter by category=Gents returns only Gents | P1 | 1. Login as Admin. 2. `GET /api/v1/products?category=Gents`. | HTTP 200. All `data` items have `category = "Gents"`. | API | |
| TC-PROD-031 | Admin | Search by article_name returns matching results | P1 | 1. Login as Admin. 2. `GET /api/v1/products?search=Busker`. | HTTP 200. All `data` items have `article_name` containing `"Busker"` (case-insensitive). Total reflects filtered count. | API | |
| TC-PROD-032 | Admin | Search by SKU substring returns matching results | P1 | 1. Login as Admin. 2. `GET /api/v1/products?search=HAWAII-BUSKER`. | HTTP 200. All items have `sku` containing `"HAWAII-BUSKER"` (case-insensitive). | API | |
| TC-PROD-033 | Admin | Filter by colour | P1 | 1. Login as Admin. 2. `GET /api/v1/products?colour=White`. | HTTP 200. All items have `colour = "White"` (case-insensitive). | API | |
| TC-PROD-034 | Admin | Filter by article_code | P1 | 1. Login as Admin. 2. `GET /api/v1/products?article_code=BSK-001`. | HTTP 200. All items have `article_code = "BSK-001"`. | API | |
| TC-PROD-035 | Admin | Combined section + category filter | P1 | 1. Login as Admin. 2. `GET /api/v1/products?section=Hawaii&category=Gents`. | HTTP 200. All items have `section = "Hawaii"` AND `category = "Gents"`. | API | |
| TC-PROD-036 | Admin | Filter with no matches returns empty list | P1 | 1. Login as Admin. 2. `GET /api/v1/products?search=ZZZNOMATCHZZZ`. | HTTP 200. `data = []`. `total = 0`. | API | |

---

### Section 3: Get Product by ID (GET /products/:id)

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-037 | Admin | GET /products/:id returns full product detail | P0 | 1. Login as Admin. 2. `GET /api/v1/products/<PRODUCT_UUID_A>`. | HTTP 200. Response `data` contains `id = PRODUCT_UUID_A`, `sku`, `article_name`, `article_code`, `colour`, `size`, `mrp`, `category`, `section`, `is_active`, `description`, `location`, `article_group`, `hsn_code`, `created_at`, `updated_at`. All fields present. | API | |
| TC-PROD-038 | Warehouse Operator | Warehouse Operator can get product by ID | P1 | 1. Login as Warehouse Operator. 2. `GET /api/v1/products/<PRODUCT_UUID_A>`. | HTTP 200. Full product detail returned. | API | |
| TC-PROD-039 | Any | GET with non-existent UUID returns 404 | P0 | 1. Login as Admin. 2. `GET /api/v1/products/00000000-0000-0000-0000-000000000000`. | HTTP 404. Error message indicating product not found. | API | |
| TC-PROD-040 | Any | GET with invalid UUID format returns 400 | P1 | 1. Login as Admin. 2. `GET /api/v1/products/not-a-uuid`. | HTTP 400. Validation error: `"Invalid product ID format"`. | API | |

---

#### 3.1 — Product sub-endpoints

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-041 | Admin | GET /products/:id/colours returns distinct colours | P1 | 1. Login as Admin. Pre-condition: Multiple colours exist for same `article_name`. 2. `GET /api/v1/products/<PRODUCT_UUID_A>/colours`. | HTTP 200. Response is an array of colour strings. Array is non-empty. Each value is a string. No duplicates. | API | Used by generate page colour picker |
| TC-PROD-042 | Admin | GET /products/:id/sizes returns sibling sizes | P1 | 1. Login as Admin. Pre-condition: Multiple sizes exist for same `article_name` + `colour`. 2. `GET /api/v1/products/<PRODUCT_UUID_A>/sizes`. | HTTP 200. Array of objects with at least `size` and `id` per entry. Sizes belong to same article+colour group. | API | Used by generate page size table |

---

### Section 4: Update Product (PUT /products/:id)

#### 4.1 — Role-based update

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-043 | Admin | Admin updates product MRP | P0 | 1. Login as Admin. 2. `PUT /api/v1/products/<PRODUCT_UUID_A>` body: `{"mrp":399}`. | HTTP 200. Response `data.mrp = "399.00"`. `updated_at` is a timestamp newer than `created_at`. | API | |
| TC-PROD-044 | Admin | Admin updates product description | P1 | 1. Login as Admin. 2. `PUT /api/v1/products/<PRODUCT_UUID_A>` body: `{"description":"Updated desc"}`. | HTTP 200. `data.description = "Updated desc"`. Other fields unchanged. | API | |
| TC-PROD-045 | Admin | Admin deactivates product | P1 | 1. Login as Admin. 2. `PUT /api/v1/products/<PRODUCT_UUID_A>` body: `{"is_active":false}`. | HTTP 200. `data.is_active = false`. | API | |
| TC-PROD-046 | Admin | Admin reactivates product | P1 | 1. Pre-condition: `PRODUCT_UUID_A` is inactive. 2. Login as Admin. 3. `PUT /api/v1/products/<PRODUCT_UUID_A>` body: `{"is_active":true}`. | HTTP 200. `data.is_active = true`. | API | |
| TC-PROD-047 | Supervisor | Supervisor updates product colour | P1 | 1. Login as Supervisor. 2. `PUT /api/v1/products/<PRODUCT_UUID_A>` body: `{"colour":"Navy"}`. | HTTP 200. `data.colour = "Navy"`. | API | |
| TC-PROD-048 | Warehouse Operator | Warehouse Operator cannot update product | P0 | 1. Login as Warehouse Operator. 2. `PUT /api/v1/products/<PRODUCT_UUID_A>` body: `{"mrp":100}`. | HTTP 403. Forbidden. `PRODUCT_UUID_A` MRP unchanged in DB. | API | |
| TC-PROD-049 | Dispatch Operator | Dispatch Operator cannot update product | P0 | 1. Login as Dispatch Operator. 2. `PUT /api/v1/products/<PRODUCT_UUID_A>` body: `{"mrp":100}`. | HTTP 403. Forbidden. | API | |

---

#### 4.2 — Update validation

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-050 | Admin | Update with negative MRP returns 400 | P0 | 1. Login as Admin. 2. `PUT /api/v1/products/<PRODUCT_UUID_A>` body: `{"mrp":-10}`. | HTTP 400. Error referencing MRP positive constraint. MRP unchanged. | API | |
| TC-PROD-051 | Admin | Update non-existent product returns 404 | P0 | 1. Login as Admin. 2. `PUT /api/v1/products/00000000-0000-0000-0000-000000000000` body: `{"mrp":100}`. | HTTP 404. Error: product not found. | API | |
| TC-PROD-052 | Admin | Update with invalid category enum returns 400 | P1 | 1. Login as Admin. 2. `PUT /api/v1/products/<PRODUCT_UUID_A>` body: `{"category":"Toddler"}`. | HTTP 400. Enum validation error for `category`. | API | |
| TC-PROD-053 | Admin | Update with invalid UUID param returns 400 | P1 | 1. Login as Admin. 2. `PUT /api/v1/products/invalid-id` body: `{"mrp":100}`. | HTTP 400. Param validation error: `"Invalid product ID format"`. | API | |

---

### Section 5: Delete Product (DELETE /products/:id — Admin only, soft-delete)

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-054 | Admin | Admin soft-deletes product | P0 | 1. Pre-condition: Create a product `PROD_DEL_UUID` via TC-PROD-001 flow. 2. Login as Admin. 3. `DELETE /api/v1/products/<PROD_DEL_UUID>`. | HTTP 200 (or 204). Product no longer appears in `GET /api/v1/products?is_active=true` or may be marked inactive/soft-deleted. `GET /api/v1/products/<PROD_DEL_UUID>` returns 404 or `is_active=false`. | API | Soft-delete: row is kept in DB but `is_active=false` or deleted flag set |
| TC-PROD-055 | Supervisor | Supervisor cannot delete product | P0 | 1. Login as Supervisor. 2. `DELETE /api/v1/products/<PRODUCT_UUID_A>`. | HTTP 403. Forbidden. Product remains in DB unchanged. | API | Only Admin may delete |
| TC-PROD-056 | Warehouse Operator | Warehouse Operator cannot delete product | P0 | 1. Login as Warehouse Operator. 2. `DELETE /api/v1/products/<PRODUCT_UUID_A>`. | HTTP 403. Forbidden. | API | |
| TC-PROD-057 | Dispatch Operator | Dispatch Operator cannot delete product | P0 | 1. Login as Dispatch Operator. 2. `DELETE /api/v1/products/<PRODUCT_UUID_A>`. | HTTP 403. Forbidden. | API | |
| TC-PROD-058 | Admin | Delete non-existent product returns 404 | P1 | 1. Login as Admin. 2. `DELETE /api/v1/products/00000000-0000-0000-0000-000000000000`. | HTTP 404. Error: product not found. | API | |
| TC-PROD-059 | Admin | Delete with invalid UUID param returns 400 | P1 | 1. Login as Admin. 2. `DELETE /api/v1/products/not-a-uuid`. | HTTP 400. Param validation error. | API | |

---

### Section 6: Image Upload (POST /products/:id/image)

#### 6.1 — Upload success cases

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-060 | Admin | Admin uploads JPEG image — 1 MB | P0 | 1. Login as Admin. 2. Prepare a JPEG file `test.jpg` of ~1 MB. 3. `POST /api/v1/products/<PRODUCT_UUID_A>/image` as multipart/form-data with field `image` = the file. | HTTP 200. Response `data` contains `image_url` (a non-empty string starting with `/` or `http`). `GET /api/v1/products/<PRODUCT_UUID_A>` response has `image_url` populated. | API | |
| TC-PROD-061 | Admin | Admin uploads PNG image | P1 | 1. Login as Admin. 2. Prepare a PNG file `test.png` (< 5 MB). 3. `POST /api/v1/products/<PRODUCT_UUID_A>/image` with `image` = the PNG. | HTTP 200. `image_url` present in response. The URL resolves to the uploaded file. | API | |
| TC-PROD-062 | Supervisor | Supervisor uploads image | P1 | 1. Login as Supervisor. 2. `POST /api/v1/products/<PRODUCT_UUID_A>/image` with valid JPEG < 5 MB. | HTTP 200. `image_url` returned. | API | |
| TC-PROD-063 | Admin | Replace existing image uploads new URL | P1 | 1. Pre-condition: `PRODUCT_UUID_A` already has an `image_url` from TC-PROD-060. 2. Login as Admin. 3. Upload a different JPEG via `POST /api/v1/products/<PRODUCT_UUID_A>/image`. | HTTP 200. New `image_url` returned (different from previous). `GET /api/v1/products/<PRODUCT_UUID_A>` shows updated `image_url`. | Integration | |

---

#### 6.2 — Upload validation & denial

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-064 | Admin | Upload file exceeding 5 MB returns 400 | P0 | 1. Login as Admin. 2. Prepare a JPEG file of 6 MB. 3. `POST /api/v1/products/<PRODUCT_UUID_A>/image` with the 6 MB file. | HTTP 400 or 413. Error indicates file too large. `image_url` not updated. | API | 5 MB limit enforced by `productImageUpload` middleware |
| TC-PROD-065 | Admin | Upload non-image file (PDF) returns 400 | P1 | 1. Login as Admin. 2. Prepare a `.pdf` file. 3. `POST /api/v1/products/<PRODUCT_UUID_A>/image` with the PDF as `image` field. | HTTP 400. Error indicates unsupported file type. No image stored. | API | |
| TC-PROD-066 | Admin | Upload to non-existent product returns 404 | P1 | 1. Login as Admin. 2. `POST /api/v1/products/00000000-0000-0000-0000-000000000000/image` with a valid JPEG. | HTTP 404. Product not found error. | API | |
| TC-PROD-067 | Warehouse Operator | Warehouse Operator cannot upload image | P0 | 1. Login as Warehouse Operator. 2. `POST /api/v1/products/<PRODUCT_UUID_A>/image` with a valid JPEG. | HTTP 403. Forbidden. `image_url` unchanged. | API | |
| TC-PROD-068 | Dispatch Operator | Dispatch Operator cannot upload image | P0 | 1. Login as Dispatch Operator. 2. `POST /api/v1/products/<PRODUCT_UUID_A>/image` with a valid JPEG. | HTTP 403. Forbidden. | API | |

---

### Section 7: E2E — Products page (Playwright)

#### 7.1 — Products list page

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-E2E-001 | Admin | Products list page loads with table | P0 | 1. Login as Admin at `http://localhost:3000`. 2. Click "Products" in the sidebar. | URL becomes `/products`. Page heading "Products" is visible. A table is displayed with columns: SKU, Article, Colour, Size, MRP, Category, Status. At least one product row is visible (from seed data). | E2E | |
| TC-PROD-E2E-002 | Admin | Search input filters table in real-time | P1 | 1. Navigate to `/products`. 2. Type "Busker" in the search input. Wait 300 ms (debounce). | Table rows update to show only rows where article name or SKU contains "Busker". Row count displayed in pagination decreases. | E2E | useDebounce hook delays the query |
| TC-PROD-E2E-003 | Admin | Section filter chips filter products | P1 | 1. Navigate to `/products`. 2. Click the "Hawaii" section chip/tab. | Table shows only products with `section = "Hawaii"`. URL or state reflects active section. Other section products not shown. | E2E | |
| TC-PROD-E2E-004 | Admin | Active/Inactive status badge is correct colour | P1 | 1. Navigate to `/products`. 2. Inspect a product row with `is_active = true`. 3. Inspect a row with `is_active = false` (must exist). | Active badge is green. Inactive badge is red or grey. Badge text matches status. | E2E | |
| TC-PROD-E2E-005 | Admin | Image thumbnail shows for products with image | P1 | 1. Pre-condition: `PRODUCT_UUID_A` has an uploaded image (TC-PROD-060). 2. Navigate to `/products`. | An `<img>` element is visible in the row for `PRODUCT_UUID_A`. The `src` attribute is a non-empty URL. No broken-image icon displayed. | E2E | |

---

#### 7.2 — Create product modal

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-E2E-006 | Admin | Create Product button opens modal | P0 | 1. Navigate to `/products` as Admin. 2. Click the "+ Create Product" or similar button (visible to managers). | A modal dialog opens with fields: Article Name, Article Code, Colour, Size, MRP, Category (dropdown), Section (dropdown), Description, Location, Article Group, HSN Code, Size From, Size To, and an image file picker. | E2E | |
| TC-PROD-E2E-007 | Admin | Single-size mode: fill Size field, submit | P0 | 1. Open create modal. 2. Fill: Article Name = "E2E Shoe", Article Code = "E2E-001", Colour = "Green", Size = "7", MRP = "150", Category = "Ladies", Section = "Hawaii". 3. Leave Size From and Size To empty. 4. Click Submit. | HTTP 201 sent to `/api/v1/products`. Success toast: "Product created successfully". Modal closes. Product "E2E Shoe" appears in the table. | E2E | |
| TC-PROD-E2E-008 | Admin | Mode conflict — both Size and Size Range filled shows error | P0 | 1. Open create modal. 2. Fill Size = "7" AND Size From = "5", Size To = "9". 3. Click Submit. | Toast error: "Enter either a single Size, OR a Size From/Size To range — not both". No API call made. Modal stays open. | E2E | Frontend validation in `handleSubmit` |
| TC-PROD-E2E-009 | Admin | Mode conflict — neither Size nor Size Range shows error | P0 | 1. Open create modal. 2. Fill Article Name, Colour, MRP etc. but leave Size, Size From, and Size To all empty. 3. Click Submit. | Toast error: "Enter either a single Size, OR a Size From/Size To range — not both". No API call made. | E2E | |
| TC-PROD-E2E-010 | Admin | Size Range mode: fill Size From/To, submit creates N products | P0 | 1. Open create modal. 2. Fill: Article Name = "Range Shoe", Article Code = "RNG-001", Colour = "Blue", MRP = "200", Category = "Gents", Section = "Merlin", Size From = "5", Size To = "9". Leave Size empty. 3. Click Submit. | Toast: "5 products created successfully". Modal closes. Five rows with "Range Shoe" appear in the table, sizes 5–9. | E2E | Calls `POST /bulk-size-range` |
| TC-PROD-E2E-011 | Admin | Size From > Size To shows error | P1 | 1. Open create modal. 2. Fill Size From = "9", Size To = "5". 3. Click Submit. | Toast error: "Size From must be less than or equal to Size To". No API call. | E2E | |
| TC-PROD-E2E-012 | Admin | Size range > 20 shows error | P1 | 1. Open create modal. 2. Fill Size From = "1", Size To = "22". 3. Click Submit. | Toast error referencing 20-size limit. No API call. | E2E | |
| TC-PROD-E2E-013 | Admin | Image file picker attached on create uploads image | P1 | 1. Open create modal. 2. Fill single-size product fields. 3. Attach a JPEG via the image file picker. 4. Click Submit. | Product created. Success toast. Image upload call made to `POST /api/v1/products/<new_id>/image`. Product row shows image thumbnail. | E2E | Image uploaded after product creation in `createProduct` mutation |
| TC-PROD-E2E-014 | Supervisor | Supervisor sees Create Product button | P1 | 1. Login as Supervisor. Navigate to `/products`. | "+ Create Product" or similar button is visible. (Managers = Admin + Supervisor in `useAuth.isManager`). | E2E | |
| TC-PROD-E2E-015 | Warehouse Operator | Warehouse Operator does not see Create Product button | P1 | 1. Login as Warehouse Operator. Navigate to `/products`. | Create Product button is NOT visible in the page header action area. | E2E | `isManager` guard hides the button |

---

#### 7.3 — Edit product modal

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-E2E-016 | Admin | Edit button opens modal pre-filled with product data | P0 | 1. Navigate to `/products`. 2. Click the Edit icon/button on the row for `PRODUCT_UUID_A`. | Edit modal opens. All fields pre-filled: Article Name = "Busker", Colour = "White", Size = "6", MRP = "299", Category = "Gents", Section = "Hawaii". Size From and Size To fields are NOT visible or are disabled (edit is always single-mode). | E2E | |
| TC-PROD-E2E-017 | Admin | Update MRP via edit modal | P0 | 1. Open edit modal for `PRODUCT_UUID_A`. 2. Change MRP to "349". 3. Click Save. | HTTP 200 sent to `PUT /api/v1/products/<PRODUCT_UUID_A>`. Success toast: "Product updated successfully". Modal closes. Row now shows MRP "₹349.00". | E2E | |
| TC-PROD-E2E-018 | Admin | Toggle active/inactive from list | P1 | 1. Navigate to `/products`. 2. Click the toggle/switch on an active product row. | Confirmation or immediate call to `PUT /api/v1/products/<id>` with `{"is_active":false}`. Status badge changes to inactive. Toast success. | E2E | `toggleStatus` function |
| TC-PROD-E2E-019 | Warehouse Operator | Warehouse Operator cannot see edit controls | P1 | 1. Login as Warehouse Operator. Navigate to `/products`. | No Edit icon or button visible on product rows. No toggle switch visible. Read-only view. | E2E | |

---

#### 7.4 — Image upload via edit modal

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-PROD-E2E-020 | Admin | Upload image from edit modal | P0 | 1. Open edit modal for `PRODUCT_UUID_A`. 2. Select a new JPEG from the image file picker. 3. Click Save. | `PUT /api/v1/products/<PRODUCT_UUID_A>` followed by `POST /api/v1/products/<PRODUCT_UUID_A>/image`. Success toast. Product row shows updated image. | E2E | |
