# Phase 13 — Dispatch

**Module:** Dispatch (multi-source: master carton, sample, e-commerce)
**Suite version:** v3
**Last updated:** 2026-04-30
**TC ID prefix:** `DISP`
**Roles under test:** Admin (`admin@binny.com / Admin@123`), Supervisor (`supervisor@binny.com / Sup@123`), Warehouse Operator (`warehouse@binny.com / Wh@123`), Dispatch Operator (`dispatch@binny.com / Dp@123`)

> **Preconditions for all API tests:** Backend running. JWT obtained via `POST /api/v1/auth/login`. API base: `http://localhost:5000/api/v1`.
> **Dependency note:** Phase 10 (master cartons), Phase 11 (samples), and Phase 12 (e-commerce) must have seeded ACTIVE/CLOSED records before running dispatch tests. Roles must exist per TC-USER-SEED-001.

---

## Table of Contents

1. [Section 1 — Schema & Constraint Validation (Zod + DB CHECK)](#section-1--schema--constraint-validation)
2. [Section 2 — Master Carton Dispatch (multi-carton)](#section-2--master-carton-dispatch)
3. [Section 3 — Sample Dispatch (1:1)](#section-3--sample-dispatch)
4. [Section 4 — E-commerce Dispatch (1:1)](#section-4--e-commerce-dispatch)
5. [Section 5 — State Changes & Audit Trail](#section-5--state-changes--audit-trail)
6. [Section 6 — List & Get Queries (source_type / source_label)](#section-6--list--get-queries)
7. [Section 7 — Role Gates (allowed and denied)](#section-7--role-gates)
8. [Section 8 — Reverse Dispatch / Un-dispatch](#section-8--reverse-dispatch--un-dispatch)
9. [Section 9 — Frontend E2E — Dispatch Page](#section-9--frontend-e2e--dispatch-page)
10. [Section 10 — Frontend E2E — Dispatches List Page](#section-10--frontend-e2e--dispatches-list-page)

---

## Section 1 — Schema & Constraint Validation

> Tests the Zod `.refine()` exactly-one-source rule and the DB-level `chk_dispatch_source_exactly_one` CHECK constraint.

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-DISP-001 | Admin | POST /dispatches with no source fields returns 400 | P0 | 1. Authenticate as Admin. 2. POST `/api/v1/dispatches` body: `{"destination":"Test City","vehicle_number":"MH01AB1234"}` (no `master_carton_ids`, `sample_record_id`, or `ecommerce_record_id`). 3. Assert status and error. | HTTP 400. Response body `success` = false. Error message contains "Exactly one dispatch source must be provided". No row inserted in `dispatch_records`. | API | Zod `.refine()` triggers when 0 sources present. |
| TC-DISP-002 | Admin | POST /dispatches with both master_carton_ids and sample_record_id returns 400 | P0 | 1. Authenticate as Admin. 2. Obtain a valid closed master carton ID (`mc_id`) and a valid ACTIVE sample record ID (`sr_id`). 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"sample_record_id":"<sr_id>"}`. | HTTP 400. Error message contains "Exactly one dispatch source must be provided". No dispatch row created. | API | Tests 2-source rejection. |
| TC-DISP-003 | Admin | POST /dispatches with all three sources returns 400 | P0 | 1. Authenticate as Admin. 2. Obtain valid IDs for a closed MC (`mc_id`), an ACTIVE sample (`sr_id`), and an ACTIVE ecommerce record (`ec_id`). 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"sample_record_id":"<sr_id>","ecommerce_record_id":"<ec_id>"}`. | HTTP 400. Error message contains "Exactly one dispatch source must be provided". | API | Tests 3-source rejection. |
| TC-DISP-004 | Admin | POST /dispatches with master_carton_ids as empty array returns 400 | P1 | 1. Authenticate as Admin. 2. POST `/api/v1/dispatches` body: `{"master_carton_ids":[]}`. | HTTP 400. Validation error references `master_carton_ids`: "At least one master carton must be selected for dispatch". | API | `master_carton_ids: []` is falsy for the refine check (length 0) so 0 sources. |
| TC-DISP-005 | Admin | POST /dispatches with master_carton_ids containing 201 items returns 400 | P1 | 1. Authenticate as Admin. 2. POST `/api/v1/dispatches` body: `{"master_carton_ids": [<201 valid UUID strings>]}`. | HTTP 400. Validation error: "Cannot dispatch more than 200 cartons at once". | API | Schema cap enforced. |
| TC-DISP-006 | Admin | POST /dispatches with invalid UUID in master_carton_ids returns 400 | P1 | 1. Authenticate as Admin. 2. POST `/api/v1/dispatches` body: `{"master_carton_ids":["not-a-uuid"]}`. | HTTP 400. Validation error: "Invalid master carton ID format". | API | |
| TC-DISP-007 | Admin | POST /dispatches with invalid UUID for sample_record_id returns 400 | P1 | 1. Authenticate as Admin. 2. POST `/api/v1/dispatches` body: `{"sample_record_id":"not-a-uuid"}`. | HTTP 400. Validation error: "Invalid sample record ID format". | API | |
| TC-DISP-008 | Admin | POST /dispatches with invalid UUID for ecommerce_record_id returns 400 | P1 | 1. Authenticate as Admin. 2. POST `/api/v1/dispatches` body: `{"ecommerce_record_id":"not-a-uuid"}`. | HTTP 400. Validation error: "Invalid ecommerce record ID format". | API | |
| TC-DISP-009 | Admin | POST /dispatches with dispatch_date in invalid format returns 400 | P1 | 1. Authenticate as Admin. 2. Obtain a valid closed MC `mc_id`. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"dispatch_date":"30-04-2026"}` (DD-MM-YYYY, not ISO 8601). | HTTP 400. Validation error: "Invalid date format, expected ISO 8601". | API | |
| TC-DISP-010 | Admin | DB CHECK constraint rejects raw INSERT with two non-null FK columns | P0 | 1. Obtain valid `master_carton_id` and `sample_record_id` in DB. 2. Execute raw SQL: `INSERT INTO dispatch_records (master_carton_id, sample_record_id, dispatched_by, dispatch_date) VALUES ('<mc_id>', '<sr_id>', '<user_id>', NOW());`. 3. Observe the error raised by the DB. | PostgreSQL raises a constraint violation error: `new row for relation "dispatch_records" violates check constraint "chk_dispatch_source_exactly_one"`. The INSERT is rejected. No row appears in `dispatch_records`. | Integration | Requires direct DB access. Proves the constraint is independent of the API. |
| TC-DISP-011 | Admin | DB CHECK constraint rejects raw INSERT with all three FK columns null | P0 | 1. Execute raw SQL: `INSERT INTO dispatch_records (dispatched_by, dispatch_date) VALUES ('<user_id>', NOW());`. | PostgreSQL check constraint violation: `chk_dispatch_source_exactly_one`. INSERT rejected. | Integration | Tests 0-source path at DB level. |
| TC-DISP-012 | Admin | lr_number exceeding 100 characters returns 400 | P2 | 1. Authenticate as Admin. 2. Obtain a valid closed MC `mc_id`. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"lr_number":"<101-char string>"}`. | HTTP 400. Validation error: "LR number must not exceed 100 characters". | API | |
| TC-DISP-013 | Admin | vehicle_number exceeding 50 characters returns 400 | P2 | 1. Authenticate as Admin. 2. Obtain a valid closed MC `mc_id`. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"vehicle_number":"<51-char string>"}`. | HTTP 400. Validation error: "Vehicle number must not exceed 50 characters". | API | |

---

## Section 2 — Master Carton Dispatch (multi-carton)

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-DISP-020 | Admin | Dispatch single CLOSED master carton succeeds and returns dispatch record | P0 | 1. Authenticate as Admin. 2. Create a master carton, pack at least 2 child boxes (status=PACKED), close it. Capture `mc_id`. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"destination":"Mumbai Warehouse","vehicle_number":"MH01AB1234","transport_details":"Fast Logistics","lr_number":"LR12345","notes":"Test dispatch"}`. 4. Assert response. | HTTP 201 (or 200). Response body is an array with 1 element. Element contains: `id` (UUID), `master_carton_id` = `mc_id`, `destination` = "Mumbai Warehouse", `vehicle_number` = "MH01AB1234", `lr_number` = "LR12345". `sample_record_id` and `ecommerce_record_id` are null. `dispatch_date` is a valid ISO timestamp. | API | |
| TC-DISP-021 | Admin | Dispatch single ACTIVE master carton (not yet closed) succeeds | P1 | 1. Authenticate as Admin. 2. Create a master carton and pack at least 1 child box (status=ACTIVE, not CLOSED). Capture `mc_id`. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"destination":"Delhi Store"}`. | HTTP 201/200. Dispatch record created. `master_carton_id` = `mc_id`. Carton is accepted from ACTIVE status. | API | Service validates ACTIVE or CLOSED status. |
| TC-DISP-022 | Admin | Dispatch CREATED (empty) master carton returns 400 | P0 | 1. Authenticate as Admin. 2. Create a new master carton but do NOT pack any boxes (status=CREATED). Capture `mc_id`. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"destination":"X"}`. | HTTP 400. Error message: "Cartons must be in ACTIVE or CLOSED status for dispatch. Invalid: BINNY-MC-<id>". No dispatch record created. Carton remains CREATED. | API | |
| TC-DISP-023 | Admin | Dispatch already DISPATCHED master carton returns 400 | P0 | 1. Authenticate as Admin. 2. Close and dispatch a master carton to get it to DISPATCHED status. 3. Attempt to dispatch the same carton again: POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"]}`. | HTTP 400. Error message contains "Cartons must be in ACTIVE or CLOSED status for dispatch. Invalid: BINNY-MC-<id>". Second dispatch record is NOT created. | API | Conflict test. |
| TC-DISP-024 | Admin | Dispatch multiple cartons in one request returns array of dispatch records | P0 | 1. Authenticate as Admin. 2. Create and close 3 distinct master cartons (IDs: `mc1`, `mc2`, `mc3`). 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc1>","<mc2>","<mc3>"],"destination":"Chennai Store"}`. 4. Assert response array length. | HTTP 201/200. Response is array of length 3. Each element has a distinct `id` and `master_carton_id` matching `mc1`, `mc2`, `mc3` respectively. All have `destination` = "Chennai Store". | API | Multi-carton dispatch — one dispatch row per carton. |
| TC-DISP-025 | Admin | Dispatch non-existent master carton ID returns 404 | P0 | 1. Authenticate as Admin. 2. POST `/api/v1/dispatches` body: `{"master_carton_ids":["00000000-0000-0000-0000-000000000000"]}`. | HTTP 404. Error: "Master cartons not found: 00000000-0000-0000-0000-000000000000". No dispatch record created. | API | Not-found error path. |
| TC-DISP-026 | Admin | Dispatch with customer_id auto-fills destination from customer delivery_location | P1 | 1. Authenticate as Admin. 2. Create a customer with `delivery_location` = "Pune Hub". Capture `cust_id`. 3. Close a master carton. 4. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"customer_id":"<cust_id>"}` (no `destination` field). | HTTP 201/200. Dispatch record's `destination` = "Pune Hub" (auto-filled from customer). `customer_id` = `cust_id`. | Integration | Auto-fill logic in `_dispatchMasterCartons`. |
| TC-DISP-027 | Admin | Dispatch with customer_id and explicit destination — explicit destination wins | P1 | 1. Authenticate as Admin. 2. Create customer with `delivery_location` = "Pune Hub". 3. Close a MC. 4. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"customer_id":"<cust_id>","destination":"Nagpur Outlet"}`. | HTTP 201/200. Dispatch record `destination` = "Nagpur Outlet" (explicit value takes precedence). | Integration | |
| TC-DISP-028 | Admin | Dispatch with custom dispatch_date stores the provided date | P1 | 1. Authenticate as Admin. 2. Close a MC. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"dispatch_date":"2026-01-15T10:00:00.000Z"}`. | HTTP 201/200. Response `dispatch_date` = "2026-01-15T10:00:00.000Z" (or equivalent ISO). DB row `dispatch_date` matches. | API | |
| TC-DISP-029 | Admin | Dispatching mix of valid and invalid carton IDs in same request rejects entire batch | P1 | 1. Authenticate as Admin. 2. Close one real MC (`mc_valid`). 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_valid>","00000000-0000-0000-0000-000000000000"]}`. | HTTP 404. Error references the missing UUID. No dispatch records created (transaction rolled back). `mc_valid` is NOT dispatched. | Integration | Tests atomicity — partial failure rolls back entire batch. |

---

## Section 3 — Sample Dispatch (1:1)

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-DISP-040 | Admin | Dispatch ACTIVE sample record succeeds | P0 | 1. Authenticate as Admin. 2. Create a sample record, add at least 2 SAMPLE-status child boxes, leave status=ACTIVE. Capture `sr_id`. 3. POST `/api/v1/dispatches` body: `{"sample_record_id":"<sr_id>","destination":"Retailer Name","vehicle_number":"KA01ZZ9999"}`. 4. Assert response. | HTTP 201/200. Response is array with 1 element. Element: `sample_record_id` = `sr_id`, `master_carton_id` = null, `ecommerce_record_id` = null, `destination` = "Retailer Name". | API | `_dispatchSample` branch. |
| TC-DISP-041 | Admin | Dispatch CLOSED sample record succeeds | P1 | 1. Authenticate as Admin. 2. Create sample record, add boxes, close it (status=CLOSED). Capture `sr_id`. 3. POST `/api/v1/dispatches` body: `{"sample_record_id":"<sr_id>","destination":"Trade Show"}`. | HTTP 201/200. Dispatch record created with `sample_record_id` = `sr_id`. | API | |
| TC-DISP-042 | Admin | Dispatch CREATED (empty) sample record returns 400 | P0 | 1. Authenticate as Admin. 2. Create a sample record but add no boxes (status=CREATED). Capture `sr_id`. 3. POST `/api/v1/dispatches` body: `{"sample_record_id":"<sr_id>"}`. | HTTP 400. Error: "Sample record must be in ACTIVE or CLOSED status for dispatch. Current status: CREATED". No dispatch row created. | API | |
| TC-DISP-043 | Admin | Dispatch already DISPATCHED sample record returns 400 | P0 | 1. Authenticate as Admin. 2. Dispatch a sample record (status → DISPATCHED). 3. Attempt second dispatch: POST `/api/v1/dispatches` body: `{"sample_record_id":"<sr_id>"}`. | HTTP 400. Error: "Sample record must be in ACTIVE or CLOSED status for dispatch. Current status: DISPATCHED". | API | Conflict test. |
| TC-DISP-044 | Admin | Dispatch non-existent sample_record_id returns 404 | P0 | 1. Authenticate as Admin. 2. POST `/api/v1/dispatches` body: `{"sample_record_id":"00000000-0000-0000-0000-000000000000"}`. | HTTP 404. Error: "Sample record not found". No dispatch row created. | API | |
| TC-DISP-045 | Admin | Sample dispatch creates exactly one dispatch_record row | P0 | 1. Authenticate as Admin. 2. Dispatch an ACTIVE sample with `sr_id`. 3. Query DB: `SELECT COUNT(*) FROM dispatch_records WHERE sample_record_id = '<sr_id>'`. | DB count = 1. The single row has `sample_record_id` = `sr_id`, `master_carton_id` = null, `ecommerce_record_id` = null. | Integration | 1:1 contract — one sample produces one dispatch row. |

---

## Section 4 — E-commerce Dispatch (1:1)

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-DISP-050 | Admin | Dispatch ACTIVE ecommerce record succeeds | P0 | 1. Authenticate as Admin. 2. Create ecommerce record, add ECOMMERCE-status child boxes, leave status=ACTIVE. Capture `ec_id`. 3. POST `/api/v1/dispatches` body: `{"ecommerce_record_id":"<ec_id>","destination":"Amazon FC","vehicle_number":"DL01AB0001"}`. | HTTP 201/200. Response array of 1 element with `ecommerce_record_id` = `ec_id`, `master_carton_id` = null, `sample_record_id` = null. | API | `_dispatchEcommerce` branch. |
| TC-DISP-051 | Admin | Dispatch CLOSED ecommerce record succeeds | P1 | 1. Authenticate as Admin. 2. Create EC record, add boxes, close it (status=CLOSED). 3. POST `/api/v1/dispatches` body: `{"ecommerce_record_id":"<ec_id>","destination":"Flipkart Hub"}`. | HTTP 201/200. Dispatch record created. | API | |
| TC-DISP-052 | Admin | Dispatch CREATED (empty) ecommerce record returns 400 | P0 | 1. Authenticate as Admin. 2. Create EC record but add no boxes (status=CREATED). 3. POST `/api/v1/dispatches` body: `{"ecommerce_record_id":"<ec_id>"}`. | HTTP 400. Error: "E-commerce record must be in ACTIVE or CLOSED status for dispatch. Current status: CREATED". | API | |
| TC-DISP-053 | Admin | Dispatch already DISPATCHED ecommerce record returns 400 | P0 | 1. Authenticate as Admin. 2. Dispatch an EC record (status → DISPATCHED). 3. Attempt second dispatch: POST `/api/v1/dispatches` body: `{"ecommerce_record_id":"<ec_id>"}`. | HTTP 400. Error: "E-commerce record must be in ACTIVE or CLOSED status for dispatch. Current status: DISPATCHED". | API | |
| TC-DISP-054 | Admin | Dispatch non-existent ecommerce_record_id returns 404 | P0 | 1. Authenticate as Admin. 2. POST `/api/v1/dispatches` body: `{"ecommerce_record_id":"00000000-0000-0000-0000-000000000000"}`. | HTTP 404. Error: "E-commerce record not found". | API | |
| TC-DISP-055 | Admin | Ecommerce dispatch creates exactly one dispatch_record row | P0 | 1. Authenticate as Admin. 2. Dispatch an ACTIVE EC record with `ec_id`. 3. Query DB: `SELECT COUNT(*) FROM dispatch_records WHERE ecommerce_record_id = '<ec_id>'`. | DB count = 1. Row has `ecommerce_record_id` = `ec_id`, `master_carton_id` = null, `sample_record_id` = null. | Integration | 1:1 contract. |

---

## Section 5 — State Changes & Audit Trail

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-DISP-060 | Admin | Master carton status becomes DISPATCHED after dispatch | P0 | 1. Authenticate as Admin. 2. Create and close a master carton. Record `mc_id`. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"]}`. 4. Query DB: `SELECT status FROM master_cartons WHERE id = '<mc_id>'`. | DB `status` = 'DISPATCHED'. `dispatched_at` is a non-null timestamp. | Integration | |
| TC-DISP-061 | Admin | Child boxes inside dispatched master carton become DISPATCHED | P0 | 1. Authenticate as Admin. 2. Create MC, pack 3 child boxes (IDs: `cb1`, `cb2`, `cb3`), close MC. 3. Dispatch the MC. 4. Query DB: `SELECT status FROM child_boxes WHERE id IN ('<cb1>','<cb2>','<cb3>')`. | All 3 rows have `status` = 'DISPATCHED'. | Integration | |
| TC-DISP-062 | Admin | Sample record status becomes DISPATCHED after sample dispatch | P0 | 1. Authenticate as Admin. 2. Create sample record, add 2 SAMPLE boxes, leave ACTIVE. Record `sr_id`. 3. POST dispatch with `sample_record_id`. 4. Query DB: `SELECT status, dispatched_at FROM sample_records WHERE id = '<sr_id>'`. | `status` = 'DISPATCHED'. `dispatched_at` is non-null timestamp. | Integration | |
| TC-DISP-063 | Admin | Sample child boxes become DISPATCHED after sample dispatch | P0 | 1. Follow TC-DISP-062 setup. After dispatch, query: `SELECT cb.status FROM sample_box_mapping sbm JOIN child_boxes cb ON cb.id = sbm.child_box_id WHERE sbm.sample_record_id = '<sr_id>' AND sbm.is_active = true`. | All child box rows have `status` = 'DISPATCHED'. | Integration | Only SAMPLE-status active-mapping boxes are updated. |
| TC-DISP-064 | Admin | Ecommerce record status becomes DISPATCHED after EC dispatch | P0 | 1. Authenticate as Admin. 2. Create EC record, add 2 ECOMMERCE boxes, leave ACTIVE. Record `ec_id`. 3. POST dispatch with `ecommerce_record_id`. 4. Query DB: `SELECT status, dispatched_at FROM ecommerce_records WHERE id = '<ec_id>'`. | `status` = 'DISPATCHED'. `dispatched_at` is non-null. | Integration | |
| TC-DISP-065 | Admin | CARTON_DISPATCHED transaction logged for each dispatched master carton | P0 | 1. Authenticate as Admin. 2. Dispatch a master carton with `mc_id`. 3. Query DB: `SELECT * FROM inventory_transactions WHERE transaction_type = 'CARTON_DISPATCHED' AND master_carton_id = '<mc_id>'`. | At least one row returned with `transaction_type` = 'CARTON_DISPATCHED', `master_carton_id` = `mc_id`, `performed_by` = the dispatching user's ID. | Integration | |
| TC-DISP-066 | Admin | CHILD_DISPATCHED transaction logged for each child box in dispatched MC | P0 | 1. Authenticate as Admin. 2. Close a MC with 3 boxes (`cb1`, `cb2`, `cb3`). Dispatch it. 3. Query DB: `SELECT child_box_id FROM inventory_transactions WHERE transaction_type = 'CHILD_DISPATCHED' AND master_carton_id = '<mc_id>'`. | 3 rows returned, one per child box. Each row `child_box_id` matches one of `cb1`, `cb2`, `cb3`. | Integration | |
| TC-DISP-067 | Admin | SAMPLE_DISPATCHED transaction logged for sample dispatch | P0 | 1. Authenticate as Admin. 2. Dispatch a sample record. 3. Query DB: `SELECT * FROM inventory_transactions WHERE transaction_type = 'SAMPLE_DISPATCHED'` ordered by `created_at DESC LIMIT 1`. | Row returned. `performed_by` = dispatching user ID. `metadata` JSON contains `sample_record_id` matching the dispatched sample. | Integration | |
| TC-DISP-068 | Admin | ECOMMERCE_DISPATCHED transaction logged for EC dispatch | P0 | 1. Authenticate as Admin. 2. Dispatch an ecommerce record. 3. Query DB: `SELECT * FROM inventory_transactions WHERE transaction_type = 'ECOMMERCE_DISPATCHED'` ordered by `created_at DESC LIMIT 1`. | Row returned. `metadata` JSON contains `ecommerce_record_id` matching the dispatched EC record. | Integration | |
| TC-DISP-069 | Admin | CHILD_DISPATCHED transaction logged for each child box in dispatched sample | P1 | 1. Authenticate as Admin. 2. Add 2 SAMPLE boxes to a sample record. Dispatch it. 3. Query DB: `SELECT child_box_id FROM inventory_transactions WHERE transaction_type = 'CHILD_DISPATCHED'` for those child box IDs. | 2 rows returned, one per child box. Each has `performed_by` = dispatching user ID. | Integration | |
| TC-DISP-070 | Admin | Dispatch transaction is atomic — partial failure rolls back all changes | P1 | 1. Authenticate as Admin. 2. Create and close 2 master cartons (`mc1`, `mc2`). Then set `mc2` status to 'DISPATCHED' directly in DB. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc1>","<mc2>"]}`. 4. Verify DB state. | HTTP 400. Error references invalid carton. `mc1` status remains unchanged (not DISPATCHED — rollback confirmed). No new `dispatch_records` rows for either MC. | Integration | Atomicity test via transaction ROLLBACK. |

---

## Section 6 — List & Get Queries

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-DISP-071 | Admin | GET /dispatches returns paginated list with source_type and source_label | P0 | 1. Authenticate as Admin. 2. Dispatch one MC, one sample, one EC record. 3. GET `/api/v1/dispatches?page=1&limit=25`. 4. Assert each record has `source_type` and `source_label`. | HTTP 200. Response contains `data` array and `total` integer. Each dispatch record has `source_type` one of: `'master_carton'`, `'sample'`, `'ecommerce'`. Each has `source_label` = the relevant barcode (`carton_barcode`, `sample_barcode`, or `ecommerce_barcode`). | API | |
| TC-DISP-072 | Admin | GET /dispatches?search=<barcode> returns matching records | P1 | 1. Authenticate as Admin. 2. Dispatch an MC with a known `carton_barcode` (e.g., `BINNY-MC-<uuid>`). 3. GET `/api/v1/dispatches?search=BINNY-MC-<uuid>`. | HTTP 200. `data` array contains the dispatch record for that carton. All returned records' `source_label` or `carton_barcode` matches the search string. | API | |
| TC-DISP-073 | Admin | GET /dispatches?from_date=&to_date= filters by dispatch_date range | P1 | 1. Authenticate as Admin. 2. Create one dispatch with `dispatch_date` = "2026-01-01T00:00:00.000Z" and one with `dispatch_date` = "2026-06-01T00:00:00.000Z". 3. GET `/api/v1/dispatches?from_date=2026-05-01&to_date=2026-07-01`. | HTTP 200. Only the June dispatch appears in `data`. The January dispatch is excluded. | API | |
| TC-DISP-074 | Admin | GET /dispatches?destination=<text> filters by destination | P1 | 1. Authenticate as Admin. 2. Dispatch to "Mumbai Warehouse" and to "Delhi Outlet". 3. GET `/api/v1/dispatches?destination=Mumbai`. | HTTP 200. Only records with `destination` ILIKE '%Mumbai%' appear. | API | |
| TC-DISP-075 | Admin | GET /dispatches/:id returns single dispatch record with source_type | P0 | 1. Authenticate as Admin. 2. Dispatch a sample record and capture dispatch `id` from response. 3. GET `/api/v1/dispatches/<id>`. 4. Assert response shape. | HTTP 200. Single object (not array). Fields: `id`, `sample_record_id` (non-null), `master_carton_id` (null), `ecommerce_record_id` (null), `source_type` = 'sample', `source_label` = the sample's barcode. | API | |
| TC-DISP-076 | Admin | GET /dispatches/:id with non-existent UUID returns 404 | P0 | 1. Authenticate as Admin. 2. GET `/api/v1/dispatches/00000000-0000-0000-0000-000000000000`. | HTTP 404. Error: "Dispatch record not found". | API | |
| TC-DISP-077 | Admin | GET /dispatches/:id with invalid UUID format returns 400 | P1 | 1. Authenticate as Admin. 2. GET `/api/v1/dispatches/not-a-uuid`. | HTTP 400. Validation error: "Invalid dispatch ID format". | API | |
| TC-DISP-078 | Admin | GET /dispatches includes customer_firm_name and article/colour/size/mrp summaries | P1 | 1. Authenticate as Admin. 2. Create a customer with `firm_name` = "Test Retailer". Dispatch MC to that customer. 3. GET `/api/v1/dispatches?search=Test+Retailer`. | HTTP 200. Matching record has `customer_firm_name` = "Test Retailer". `article_summary`, `colour_summary`, `size_summary`, `mrp_summary` fields are present and non-null when boxes exist. | API | Summary fields from LATERAL subquery. |
| TC-DISP-079 | Admin | GET /dispatches pagination — page 2 returns different records than page 1 | P1 | 1. Authenticate as Admin. 2. Ensure at least 30 dispatch records exist. 3. GET `/api/v1/dispatches?page=1&limit=10`. Capture IDs. 4. GET `/api/v1/dispatches?page=2&limit=10`. Capture IDs. | Both pages return 10 records each. No overlap in IDs between page 1 and page 2. `total` is consistent across both calls. | API | |
| TC-DISP-080 | Any | GET /dispatches unauthenticated returns 401 | P0 | 1. GET `/api/v1/dispatches` without `Authorization` header. | HTTP 401. No data returned. | API | Route requires `authenticate` middleware. |

---

## Section 7 — Role Gates

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-DISP-081 | Supervisor | Supervisor can POST /dispatches (master carton) | P0 | 1. Authenticate as Supervisor. 2. Close a master carton. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"destination":"Test"}`. | HTTP 201/200. Dispatch record created. `dispatched_by` in DB = Supervisor's user ID. | API | |
| TC-DISP-082 | Dispatch Operator | Dispatch Operator can POST /dispatches (sample) | P0 | 1. Authenticate as Dispatch Operator. 2. Add boxes to a sample record and leave it ACTIVE. 3. POST `/api/v1/dispatches` body: `{"sample_record_id":"<sr_id>","destination":"Test"}`. | HTTP 201/200. Dispatch created. | API | |
| TC-DISP-083 | Dispatch Operator | Dispatch Operator can POST /dispatches (ecommerce) | P0 | 1. Authenticate as Dispatch Operator. 2. Add boxes to EC record, leave ACTIVE. 3. POST `/api/v1/dispatches` body: `{"ecommerce_record_id":"<ec_id>","destination":"Test"}`. | HTTP 201/200. Dispatch created. | API | |
| TC-DISP-084 | Warehouse Operator | Warehouse Operator cannot POST /dispatches — 403 | P0 | 1. Authenticate as Warehouse Operator. 2. Obtain a valid closed MC ID. 3. POST `/api/v1/dispatches` body: `{"master_carton_ids":["<mc_id>"],"destination":"Test"}`. | HTTP 403. Error: "Access denied" or "Insufficient permissions". No dispatch record created. MC remains in its prior status. | API | Warehouse is excluded from `authorize(ADMIN, SUPERVISOR, DISPATCH_OPERATOR)`. |
| TC-DISP-085 | Warehouse Operator | Warehouse Operator GET /dispatches is allowed (no role restriction) | P1 | 1. Authenticate as Warehouse Operator. 2. GET `/api/v1/dispatches`. | HTTP 200. List returned. The GET route has no `authorize()` call — only `authenticate`. | API | Read access is not role-gated for GET. |
| TC-DISP-086 | Dispatch Operator | Dispatch Operator GET /dispatches/:id is allowed | P1 | 1. Authenticate as Dispatch Operator. 2. Obtain a valid dispatch `id`. 3. GET `/api/v1/dispatches/<id>`. | HTTP 200. Dispatch record returned. | API | |

---

## Section 8 — Reverse Dispatch / Un-dispatch

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-DISP-090 | Admin | Reverse dispatch / un-dispatch endpoint does not exist (expected 404) | P1 | 1. Authenticate as Admin. 2. Dispatch a master carton, capture dispatch `id`. 3. DELETE `/api/v1/dispatches/<id>` (or POST `/api/v1/dispatches/<id>/undo`). | HTTP 404. No route registered for DELETE or undo. The dispatch record and statuses remain unchanged. | API | Feature not implemented. If a future commit adds it, this TC should be replaced with positive and negative coverage. |

---

## Section 9 — Frontend E2E — Dispatch Page

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-DISP-E2E-001 | Admin | Dispatch page renders three source-type tabs | P0 | 1. Log in as Admin at `http://localhost:3000`. 2. Navigate to `/dispatch`. 3. Inspect the tab bar. | Three tabs visible in order: "Master Carton" (Package icon), "Sample" (FlaskConical icon), "E-commerce" (ShoppingCart icon). Active tab has `border-binny-navy text-binny-navy` underline. Page title is "Dispatch". | E2E | |
| TC-DISP-E2E-002 | Admin | Master Carton tab is selected by default | P0 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Assert which tab is active and which panel is visible. | "Master Carton" tab is active (has navy underline). The "Scan Master Cartons" panel is visible on the right side. No Sample or E-commerce panel visible. | E2E | |
| TC-DISP-E2E-003 | Admin | Switching to Sample tab shows Sample panel and hides Carton panel | P1 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Click the "Sample" tab. 4. Assert panel visibility. | "Sample" tab becomes active. "Scan Sample" panel (red accent, FlaskConical icon) is visible. The "Scan Master Cartons" and carton list panel are hidden. "Or enter sample barcode manually" input with placeholder "BINNY-SR-..." is present. "Find" button is present. | E2E | |
| TC-DISP-E2E-004 | Admin | Switching to E-commerce tab shows EC panel | P1 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Click the "E-commerce" tab. 4. Assert panel. | "E-commerce" tab active. "Scan E-commerce Record" panel (purple accent) visible. Input placeholder "BINNY-EC-..." present. "Find" button present. | E2E | |
| TC-DISP-E2E-005 | Admin | Master Carton tab — manually enter valid carton barcode and add to list | P0 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Ensure MC tab active. 4. Type a valid ACTIVE/CLOSED carton barcode into the manual entry input. 5. Click "Add" button (or press Enter). 6. Assert carton list. | Toast "Added carton: BINNY-MC-<id>" appears. The "Cartons to Dispatch" list now shows the carton with barcode, status badge, and child box count. Remove (X) button is visible per carton. | E2E | Requires existing carton in test DB. |
| TC-DISP-E2E-006 | Admin | Adding same carton twice shows "Carton already added" error | P1 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Add a valid carton once (successfully added). 4. Attempt to add the same barcode again. | Toast error "Carton already added". The list count does not increase (duplicate not added). | E2E | |
| TC-DISP-E2E-007 | Admin | Adding DISPATCHED carton shows "already been dispatched" error toast | P1 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Enter barcode of a carton that is already DISPATCHED. 4. Click "Add". | Toast error "This carton has already been dispatched". Carton is NOT added to the list. | E2E | |
| TC-DISP-E2E-008 | Admin | Adding CREATED carton shows "empty" error toast | P1 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Enter barcode of a CREATED-status carton. 4. Click "Add". | Toast error "This carton is empty (CREATED status). Pack boxes first." Carton NOT added. | E2E | |
| TC-DISP-E2E-009 | Admin | Remove carton from list via X button | P1 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Add 2 cartons to the list. 4. Click the X button on the first carton. | First carton removed from list. List now shows 1 carton. "Create Dispatch (1 carton)" submit button label updated. | E2E | |
| TC-DISP-E2E-010 | Admin | Submit button disabled when no cartons added (MC tab) | P0 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Ensure MC tab active and carton list is empty. 4. Inspect submit button. | "Create Dispatch (0 cartons)" button has `disabled` attribute. Clicking it shows toast "Add at least one master carton". | E2E | |
| TC-DISP-E2E-011 | Admin | Sample tab — enter valid sample barcode and confirm selection appears | P0 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Click Sample tab. 4. Enter valid ACTIVE sample barcode in input. 5. Click "Find". | Toast "Sample found: <sample name>". Red selection card appears below input showing sample name, barcode (font-mono), recipient/purpose, box count, status. X button to clear. | E2E | |
| TC-DISP-E2E-012 | Admin | E-commerce tab — enter valid EC barcode and confirm selection appears | P0 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Click E-commerce tab. 4. Enter valid ACTIVE EC barcode. 5. Click "Find". | Toast "E-commerce record found: <name>". Purple selection card appears showing name, barcode, marketplace, order_reference, listing_sku (when present), box count, status. | E2E | |
| TC-DISP-E2E-013 | Admin | Successful master carton dispatch redirects to /dispatches | P0 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Add at least 1 valid CLOSED carton. 4. Fill destination field. 5. Click submit. | Loading spinner shows on button. On success: toast "Dispatch created successfully". Browser redirects to `/dispatches`. Carton list and form fields are cleared. | E2E | |
| TC-DISP-E2E-014 | Admin | Open Scanner / Hide Scanner toggle in MC tab | P1 | 1. Log in as Admin. 2. Navigate to `/dispatch`. 3. Ensure MC tab active. 4. Click "Open Scanner" button. 5. Assert scanner visible. 6. Click "Hide Scanner". 7. Assert scanner hidden. | Clicking "Open Scanner" renders QRScanner component. Button label changes to "Hide Scanner". Clicking "Hide Scanner" removes QRScanner. Button reverts to "Open Scanner". | E2E | |
| TC-DISP-E2E-015 | Warehouse Operator | Warehouse Operator navigating to /dispatch page can view it but dispatch API returns 403 | P0 | 1. Log in as Warehouse Operator. 2. Navigate to `/dispatch`. 3. Add a valid carton. 4. Click submit. | Page renders (no RBAC guard on frontend route). On submit, API returns 403. Toast error message appears. No redirect. Carton list remains unchanged. | E2E | Frontend shows page; role enforcement is server-side. |

---

## Section 10 — Frontend E2E — Dispatches List Page

| TC ID | Role | Title | Priority | Steps | Expected Result | Type | Notes |
|---|---|---|---|---|---|---|---|
| TC-DISP-E2E-020 | Admin | Dispatches list page loads and groups by customer | P0 | 1. Log in as Admin. 2. Navigate to `/dispatches`. 3. Assert page structure. | Page title "Dispatches". Cards grouped by customer. Each group shows customer name (or "Walk-in / No Customer"), total dispatch count, box count, latest date. Collapsible chevron icon per group. | E2E | |
| TC-DISP-E2E-021 | Admin | Expanding a customer group shows individual dispatch records with SourceTypeBadge | P0 | 1. Log in as Admin. 2. Navigate to `/dispatches`. 3. Click on a customer group row to expand. 4. Assert individual records. | Group expands showing individual dispatch cards. Each card displays: SourceTypeBadge (gray="Master Carton", red="Sample", purple="E-commerce"), `source_label` in font-mono below badge, dispatch date, destination, vehicle number, LR number (if present). | E2E | |
| TC-DISP-E2E-022 | Admin | Source type filter dropdown filters list client-side | P1 | 1. Log in as Admin. 2. Ensure dispatches of all 3 source types exist. 3. Navigate to `/dispatches`. 4. Select "Sample" from the source type dropdown. 5. Assert visible groups. | Only customer groups containing Sample dispatch records are shown. Master Carton and E-commerce records are hidden from the grouped view. Changing to "All Source Types" restores full list. | E2E | Filter is client-side (`useMemo`). |
| TC-DISP-E2E-023 | Admin | Search bar filters by barcode, destination, LR number | P1 | 1. Log in as Admin. 2. Navigate to `/dispatches`. 3. Type the LR number of a specific dispatch. 4. Assert results. | Customer groups containing the matching LR number dispatch are shown. Other groups are hidden or their records are absent. Clearing search restores full list. | E2E | Search hits API via `?search=` param. |
| TC-DISP-E2E-024 | Admin | Date range filter narrows list by from_date / to_date | P1 | 1. Log in as Admin. 2. Navigate to `/dispatches`. 3. Set "From" date to tomorrow and "To" date to next week. 4. Assert empty state or future dispatches only. | Records with dispatch_date before tomorrow are excluded. Empty state shows "No dispatch records found." with ClipboardList icon if no future dispatches exist. | E2E | |
| TC-DISP-E2E-025 | Admin | Pagination controls appear when total records exceed PAGE_SIZE | P1 | 1. Log in as Admin. 2. Ensure more than PAGE_SIZE dispatch records exist. 3. Navigate to `/dispatches`. 4. Assert pagination section. | Pagination bar appears at bottom: "Showing X to Y of Z" text. "Previous" button disabled on page 1. "Next" button enabled. Clicking "Next" loads page 2. | E2E | |
| TC-DISP-E2E-026 | Admin | "Dispatch Carton" action button in header navigates to /dispatch | P1 | 1. Log in as Admin. 2. Navigate to `/dispatches`. 3. Click "Dispatch Carton" button in page header. | Browser navigates to `/dispatch`. | E2E | |
| TC-DISP-E2E-027 | Admin | SourceTypeBadge renders correct color for each source type | P0 | 1. Log in as Admin. 2. Navigate to `/dispatches`. 3. Expand a customer group containing all three source types. 4. Inspect badge colors. | Master Carton badge: `variant="gray"` (gray text). Sample badge: `variant="red"` (red). E-commerce badge: `variant="purple"` (purple). Each badge has the correct label text matching its type. | E2E | |
